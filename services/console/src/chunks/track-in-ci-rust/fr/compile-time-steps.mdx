import StepsForBencherCloud from "./steps-for-bencher-cloud.mdx";
import StepsForBencherSelfHosted from "./steps-for-bencher-self-hosted.mdx";

<StepsForBencherCloud
  step={<>Compilez votre code {props.lang} avec la sous-commande <code><a href="/fr/docs/explanation/bencher-run/">bencher run</a></code> dans votre flux de travail CI en utilisant <a href={props.flagUrl}>l'option <code>--build-time</code></a> avec <a href={props.adapterUrl}>l'adaptateur <code>json</code></a>.</>}
/>

<StepsForBencherSelfHosted
  step={<>Compilez votre code {props.lang} avec la sous-commande <code><a href="/fr/docs/explanation/bencher-run/">bencher run</a></code> dans votre flux de travail CI en utilisant <a href={props.flagUrl}>l'option <code>--build-time</code></a> avec <a href={props.adapterUrl}>l'adaptateur <code>json</code></a> et en définissant l'option <a href="/fr/docs/explanation/bencher-run/#--host-url"><code>--host</code></a> sur l'URL de votre instance Bencher Self-Hosted.</>}
/>