import StepsForBencherCloud from "./steps-for-bencher-cloud.mdx";
import StepsForBencherSelfHosted from "./steps-for-bencher-self-hosted.mdx";

<StepsForBencherCloud
  step={<>{props.lang} 코드를 CI 워크플로에서 <code><a href="/ko/docs/explanation/bencher-run/">bencher run</a></code> 서브 커맨드를 사용하여 <a href={props.flagUrl}> <code>--build-time</code> 플래그</a>와 <a href={props.adapterUrl}><code>json</code> 어댑터</a>를 사용하여 컴파일합니다.</>}
/>

<StepsForBencherSelfHosted
  step={<>{props.lang} 코드를 CI 워크플로에서 <code><a href="/ko/docs/explanation/bencher-run/">bencher run</a></code> 서브 커맨드를 사용하여 <a href={props.flagUrl}> <code>--build-time</code> 플래그</a>와 <a href={props.adapterUrl}> <code>json</code> 어댑터</a>를 사용하고 <a href="/ko/docs/explanation/bencher-run/#--host-url"> <code>--host</code> 옵션</a>을 Bencher Self-Hosted 인스턴스 URL로 설정하여 컴파일합니다.</>}
/>