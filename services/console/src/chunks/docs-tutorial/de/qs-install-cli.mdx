import CliUnixScript from "../../general/cli-unix-script.mdx";
import CliWindowsScript from "../../general/cli-windows-script.mdx";
import QSCliVersionOutput from "../qs-cli-version-output.mdx";

## Installation des `bencher` CLI

### Linux, Mac, & Unix
Für Linux, Mac und andere Unix-ähnliche Systeme führen Sie Folgendes in Ihrem Terminal aus:

<CliUnixScript />

### Windows
Für Windows-Systeme führen Sie Folgendes in einem _PowerShell_-Terminal aus:

<CliWindowsScript />

<br />

> 🐰 Wenn Sie einen Fehler erhalten, der besagt `Das Ausführen von Skripten ist auf diesem System deaktiviert`:
> - `Öffnen Sie Powershell` mit `Als Administrator ausführen`
> - Ausführen: `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned`
> - Eingeben: `Y`
> - Führen Sie dieses Skript erneut aus

Für weitere Installationsmöglichkeiten, siehe [wie man das `bencher` CLI installiert](/de/docs/how-to/install-cli).

Nun, testen wir, ob wir das `bencher` CLI installiert haben.

Führen Sie aus: `bencher --version`

Sie sollten sehen:

<QSCliVersionOutput />