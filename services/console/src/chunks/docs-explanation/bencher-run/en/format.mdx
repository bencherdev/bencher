### `--format <FORMAT>`

<br />

Optional: Format for the final Report.
The default is `human`.

Possible values:
    - `human`: Human-readable format
    - `json`: JSON format
    - `html`: HTML format