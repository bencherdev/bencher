## 🦀 Rust Iai-Callgrind

O Adaptador Rust Iai (`rust_iai_callgrind`) espera a saída do [Iai-Callgrind](https://github.com/iai-callgrind/iai-callgrind).
As Medidas `instructions`, `l1_access`, `l2_access`, `ram_access`, `total_accesses` e `estimated_cycles` são coletadas.
Apenas estas medidas (ou seja, `value`) estão disponíveis. Não existem medidas `lower_value` e `upper_value`.
As Medidas para este adaptador não são criadas por padrão para todos os projetos.
No entanto, quando você usa este adaptador, essas Medidas serão automaticamente criadas para o seu Projeto.