import DockerCompose from "../docker-compose.mdx";
import BencherUp from "./bencher-up.mdx";
import BencherLogs from "./bencher-logs.mdx";
import BencherDown from "./bencher-down.mdx";

## Deployment

A maneira mais fácil de implantar o Bencher Self-Hosted é utilizando um tríptico de subcomandos da CLI `bencher`:

- [`bencher up`][bencher up]: Inicia os contêineres do Bencher Self-Hosted.
  Isso é semelhante a executar [`docker compose up`][docker compose up].
- [`bencher logs`][bencher logs]: Exibir a saída dos contêineres do Bencher Self-Hosted,
  se `bencher up` foi executado com [a flag `--detach`][detach flag].
  Isso é semelhante a executar [`docker compose logs`][docker compose logs].
- [`bencher down`][bencher down]: Parar e remover os contêineres do Bencher Self-Hosted,
  se `bencher up` foi executado com [a flag `--detach`][detach flag].
  Isso é semelhante a executar [`docker compose down`][docker compose down].

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/
[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/
[docker compose down]: https://docs.docker.com/reference/cli/docker/compose/down/

[bencher up]: #bencher-up
[bencher logs]: #bencher-logs
[bencher down]: #bencher-down
[detach flag]: #--detach

<div class="box">
  <BencherUp />
</div>

<div class="box">
  <BencherLogs />
</div>

<div class="box">
  <BencherDown />
</div>

Alternativamente, você pode apenas usar o `docker compose` para orquestrar os contêineres.
Este é um exemplo de arquivo `docker-compose.yml`:

<DockerCompose />