### サービス

実行するコンテナを選択します。
選択肢は `all`、`api`、`console` です。
デフォルトでは `all` が選択されます。
[`docker compose down`][docker compose logs] の `SERVICE` 引数に似ています。

[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/