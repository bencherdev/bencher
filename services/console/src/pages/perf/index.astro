---
export const prerender = false;

import PerfLayout from "../../layouts/perf/PerfLayout.astro";
import PublicProjects from "../../components/perf/PublicProjects";

// Using `meta.env` requires `prerender = false`
const BENCHER_API_URL = import.meta.env.BENCHER_API_URL;
const title = "Bencher Projects";
---

<PerfLayout
  title={title}
  titleFmt={(title) => `📈 ${title}`}
  description="View continuous benchmarking results and performance regressions for public projects on Bencher"
  canonical="/perf"
>
  <PublicProjects client:only="solid-js" apiUrl={BENCHER_API_URL} />
</PerfLayout>
