import CargoRustIaiCallgrind from "../cargo-rust-iai-callgrind.mdx";

## 🦀 Rust Iai-Callgrind

Der Rust Iai Adapter (`rust_iai_callgrind`) erwartet eine Ausgabe von [Iai-Callgrind](https://github.com/iai-callgrind/iai-callgrind).
Die Maße `instructions`, `l1-hits`, `l2-hits`, `ram-hits`, `total-read-write` und `estimated-cycles` werden gesammelt.
Nur diese Maße (d.h. `value`) sind verfügbar. Es gibt keine Maße für `lower_value` und `upper_value`.
Die Maße für diesen Adapter werden nicht standardmäßig für alle Projekte erstellt.
Wenn Sie diesen Adapter jedoch verwenden, werden diese Maße automatisch für Ihr Projekt erstellt.

<CargoRustIaiCallgrind />