---
import { otherLanguages } from "../../../../i18n/ui";
export async function getStaticPaths() {
	return otherLanguages.map((lang) => {
		return { params: { lang } };
	});
}
const { lang } = Astro.params;
---
<meta http-equiv="refresh" content={`0;url=/${lang}/docs/`} />