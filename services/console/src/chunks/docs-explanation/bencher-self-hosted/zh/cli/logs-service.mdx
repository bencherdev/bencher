### 服务

选择要运行的容器。
可能的选择有：`all`、`api` 和 `console`。
默认选择是 `all`。
类似于[`docker compose logs`][docker compose logs]的 `SERVICE` 参数。

[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/