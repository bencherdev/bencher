---
import DocsLayout from "./DocsLayout.astro";
import DocsTab from "../../components/docs/docs_tab";
import type { Author } from "../../components/docs/author";

interface Props {
  title: string;
  titleFmt?: undefined | ((title: string) => string);
  description: string;
  heading?: string;
  lang?: string;
  author?: Author;
}

const { title, titleFmt, description, heading, lang, author } = Astro.props;
---

<DocsLayout
  title={title}
  titleFmt={titleFmt ?? ((title) => `🧑‍💻 ${heading ?? title}`)}
  description={description}
  heading={heading}
  tab={DocsTab.Learn}
  lang={lang}
  author={author}
>
  <slot />
</DocsLayout>
