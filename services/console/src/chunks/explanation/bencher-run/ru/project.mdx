### `--project <PROJECT>`

<br />

Вам необходимо установить либо опцию `--project`, либо переменную окружения `BENCHER_PROJECT`
для указания ссылки или UUID уже существующего проекта.
Если оба определены, то значение опции `--project` имеет приоритет перед переменной окружения `BENCHER_PROJECT`.