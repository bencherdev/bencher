import StepsForBencherCloud from "./steps-for-bencher-cloud.mdx";
import StepsForBencherSelfHosted from "./steps-for-bencher-self-hosted.mdx";

<StepsForBencherCloud
  step={<>在您的 CI 工作流中使用 <code><a href="/zh/docs/explanation/bencher-run/">bencher run</a></code> 子命令运行您的基准测试，使用 <a href={props.url}>这个 <code>{props.adapter}</code> 适配器</a>。</>}
/>

<StepsForBencherSelfHosted
  step={<>使用 <code><a href="/zh/docs/explanation/bencher-run/">bencher run</a></code> 子命令在您的 CI 工作流程中运行基准测试，使用 <a href={props.url}> <code>{props.adapter}</code> 适配器</a> 并设置 <a href="/zh/docs/explanation/bencher-run/#--host-url"><code>--host</code> 选项</a> 为您的 Bencher 自托管实例的 URL。</>}
/>