import BencherRunCode from "../bencher-run-code.mdx";

## Host URL

在使用 Bencher 自托管时，您需要为大多数 `bencher` CLI 子命令将 [the `--host` option][host option] 设置为您的 Bencher API 服务器的 URL。

例如，如果您使用 <code><a href="/zh/docs/explanation/bencher-run/">bencher run</a></code> CLI 子命令，并与 Bencher API 服务器（设为 `https://api.bencher.example.com`）配合使用，那么您可以这样写：

<BencherRunCode />

[host option]: /zh/docs/explanation/bencher-run/#--host-url
