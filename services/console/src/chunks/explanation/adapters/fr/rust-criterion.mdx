## 🦀 Critère Rust

L'adaptateur Critère Rust (`rust_criterion`) attend une sortie [Criterion](https://github.com/bheisler/criterion.rs).
La mesure de `latence` (c'est-à-dire `nanosecondes (ns)`) est recueillie.
Les `lower_value` et `upper_value` sont les limites inférieure et supérieure de la pente (si disponible) ou de la moyenne (sinon) (c'est-à-dire `valeur`) respectivement.