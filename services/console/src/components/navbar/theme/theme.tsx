export const THEME_KEY = "theme";

export enum Theme {
    Light = "light",
    Dark = "dark",
}

export const THEME_TOGGLE_ID = "theme-toggle";

export enum ThemeId {
	Light = "light-theme",
	Dark = "dark-theme",
}