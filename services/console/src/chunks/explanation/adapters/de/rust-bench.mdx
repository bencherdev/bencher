import CargoRustBench from "../cargo-rust-bench.mdx";

## 🦀 Rust Bench

Der Rust Bench Adapter (`rust_bench`) erwartet eine Ausgabe von [libtest bench](https://doc.rust-lang.org/rustc/tests/index.html#benchmarks).
Die `latency` Messung (d.h. `Nanosekunden (ns)`) wird erfasst.
Der `lower_value` und `upper_value` sind die Abweichungen unterhalb und oberhalb des Medians (d.h. `value`) jeweils.

<CargoRustBench />