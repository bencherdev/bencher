Depending on your use case, you can set up [Continuous Benchmarking][continuous benchmarking] in GitLab CI/CD for your:

- [Target Branch](#target-branch)
- [Merge Requests](#merge-requests)

Make sure you have [created a project API key][created an api token]
and set it as a masked variable named `BENCHER_API_KEY` before continuing on!
Navigate to `Your Repo -> Settings -> CI/CD -> Variables -> Expand -> Add variable`.
The variable key should be `BENCHER_API_KEY` and the variable value should be your project API key.
Check both the `Protect variable` and `Mask variable` boxes.

[continuous benchmarking]: /docs/explanation/continuous-benchmarking/
[created an api token]: /docs/how-to/claim/#create-an-api-token

[target branch]: #target-branch
[merge requests]: #merge-requests