import StepsForBencherCloud from "./steps-for-bencher-cloud.mdx";
import StepsForBencherSelfHosted from "./steps-for-bencher-self-hosted.mdx";

<StepsForBencherCloud
  step={<>在您的 CI 工作流中使用 <a href={props.flagUrl}> <code>--build-time</code> 标志</a>和<a href={props.adapterUrl}> <code>json</code> 适配器</a>通过 <code><a href="/zh/docs/explanation/bencher-run/">bencher run</a></code> 子命令编译您的 {props.lang} 代码。</>}
/>

<StepsForBencherSelfHosted
  step={<>在您的 CI 工作流中使用 <a href={props.flagUrl}> <code>--build-time</code> 标志</a>和<a href={props.adapterUrl}> <code>json</code> 适配器</a>通过 <code><a href="/zh/docs/explanation/bencher-run/">bencher run</a></code> 子命令编译您的 {props.lang} 代码，并设置<a href="/zh/docs/explanation/bencher-run/#--host-url"> <code>--host</code> 选项</a>为您的 Bencher Self-Hosted 实例 URL。</>}
/>