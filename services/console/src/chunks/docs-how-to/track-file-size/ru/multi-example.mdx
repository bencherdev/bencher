import MultiExample from "../multi-example.mdx";
import MultiJsonExample from "../multi-json-example.mdx";

Размер файла всегда будет целым числом в байтах.
То есть, он _никогда_ не будет дробным, например `42.5`.
Тем не менее, BMF JSON должен поддерживать широкий диапазон возможных значений,
поэтому он использует числа с плавающей точкой вместо целых чисел.

Если вы хотите отслеживать размеры нескольких файлов,
вы можете просто использовать опцию `--file-size` несколько раз в одной и той же
<code><a href="/ru/docs/explanation/bencher-run/">bencher run</a></code> команде.
Например, вы можете отслеживать как `my_binary`, так и версию для Windows, `my_binary.exe`.

<MultiExample />

Сгенерированный BMF JSON будет выглядеть так, если размер `my_binary.exe` составляет `48` байт:

<MultiJsonExample />