import StepsForBencherCloud from "./steps-for-bencher-cloud.mdx";
import StepsForBencherSelfHosted from "./steps-for-bencher-self-hosted.mdx";

<StepsForBencherCloud
  step={<>CI ワークフロー内で <code><a href="/ja/docs/explanation/bencher-run/">bencher run</a></code> サブコマンドを使ってベンチマークを実行し、 <a href={props.url}> <code>{props.adapter}</code> アダプター</a>を使用する。</>}
/>

<StepsForBencherSelfHosted
  step={<>次のサブコマンドを使用して CI ワークフローでベンチマークを実行します: <code><a href="/ja/docs/explanation/bencher-run/">bencher run</a></code> <a href={props.url}><code>{props.adapter}</code> アダプター</a>を使用して、 <a href="/ja/docs/explanation/bencher-run/#--host-url"><code>--host</code> オプション</a> を Bencher Self-Hosted インスタンスの URL に設定します。</>}
/>