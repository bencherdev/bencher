В зависимости от вашего варианта использования, вы можете настроить [Непрерывное Бенчмарк-тестирование][continuous benchmarking] в GitLab CI/CD для:

- [Целевой ветки](#target-branch)
- [Запросов на слияние](#merge-requests)

Убедитесь, что вы [создали токен API][created an api token]
и задали его как переменную с маской под именем `BENCHER_API_TOKEN` перед тем, как продолжить!
Перейдите в `Ваш репозиторий -> Настройки -> CI/CD -> Переменные -> Развернуть -> Добавить переменную`.
Ключ переменной должен быть `BENCHER_API_TOKEN`, а значение переменной должно быть вашим токеном API.
Отметьте оба поля `Защитить переменную` и `Скрыть переменную`.

[continuous benchmarking]: /ru/docs/explanation/continuous-benchmarking/
[created an api token]: /ru/docs/how-to/claim/#create-an-api-token

[target branch]: #target-branch
[merge requests]: #merge-requests