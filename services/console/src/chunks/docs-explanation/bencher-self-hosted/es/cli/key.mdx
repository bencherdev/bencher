### `--key <KEY>`

O bien la opción `--key` o la variable de entorno `BENCHER_API_KEY` debe configurarse con una clave de API de usuario válida (`bencher_user_*`). Si se especifican ambos, la opción `--key` tiene prioridad sobre la variable de entorno `BENCHER_API_KEY`.
