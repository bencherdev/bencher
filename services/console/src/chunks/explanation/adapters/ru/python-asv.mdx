## 🐍 Python ASV

Адаптер Python ASV (`python_asv`) работает с выводом командной строки [airspeed velocity](https://github.com/airspeed-velocity/asv) ([asv run](https://asv.readthedocs.io/en/stable/commands.html#asv-run)).
Измеряется `latency` (то есть `наносекунды (ns)`).
`lower_value` и `upper_value` — это интерквартильный размах ниже и выше медианы (то есть `value`) соответственно.