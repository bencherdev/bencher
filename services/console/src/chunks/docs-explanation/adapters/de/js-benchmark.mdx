import NodeJsBenchmark from "../node-js-benchmark.mdx";

## 🕸 JavaScript-Benchmark

Der JavaScript-Benchmark-Adapter (`js_benchmark`) erwartet eine Ausgabe von [Benchmark.js](https://github.com/bestiejs/benchmark.js).
Die `throughput` Messung (d.h. `Operationen / Sekunde (ops/sec)`) wird gesammelt.
Die `lower_value` und `upper_value` sind die relative Fehlerspanne unterhalb und oberhalb des Medianwerts (d.h. `value`) bzw.

<NodeJsBenchmark />