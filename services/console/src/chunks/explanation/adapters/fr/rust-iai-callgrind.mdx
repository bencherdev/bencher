## 🦀 Rust Iai-Callgrind

L'adaptateur Rust Iai (`rust_iai_callgrind`) nécessite une sortie [Iai-Callgrind](https://github.com/iai-callgrind/iai-callgrind).
Les mesures `instructions`, `l1_access`, `l2_access`, `ram_access`, `total_accesses` et `estimated_cycles` sont recueillies.
Seulement ces mesures (c'est-à-dire `value`) sont disponibles. Il n'y a pas de mesures de `lower_value` et `upper_value`.
Les mesures pour cet adaptateur ne sont pas créées par défaut pour tous les projets.
Cependant, lorsque vous utilisez cet adaptateur, ces mesures seront automatiquement créées pour votre Projet.