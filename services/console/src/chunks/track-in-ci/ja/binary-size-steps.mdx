import StepsForBencherCloud from "./steps-for-bencher-cloud.mdx";
import StepsForBencherSelfHosted from "./steps-for-bencher-self-hosted.mdx";

<StepsForBencherCloud
  step={<>CIワークフローで<code><a href="/ja/docs/explanation/bencher-run/">bencher run</a></code>サブコマンドを使用して{props.lang}コードをコンパイルします。<a href={props.optionUrl}><code>--file-size</code>オプション</a>をバイナリの出力パスに設定し、<a href={props.adapterUrl}><code>json</code>アダプター</a>を使用します。</>}
/>

<StepsForBencherSelfHosted
  step={<>CIワークフローで<code><a href="/ja/docs/explanation/bencher-run/">bencher run</a></code>サブコマンドを使用して{props.lang}コードをコンパイルします。<a href={props.optionUrl}><code>--file-size</code>オプション</a>をバイナリの出力パスに設定し、<a href={props.adapterUrl}><code>json</code>アダプター</a>を使用し、<a href="/ja/docs/explanation/bencher-run/#--host-url"><code>--host</code>オプション</a>をBencher Self-HostedインスタンスのURLに設定します。</>}
/>