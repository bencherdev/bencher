### Сервис

Выберите контейнеры для запуска. Возможные варианты: `all`, `api` и `console`. По умолчанию выбран `all`. Аналогично аргументу `SERVICE` для [`docker compose down`][docker compose logs].

[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/