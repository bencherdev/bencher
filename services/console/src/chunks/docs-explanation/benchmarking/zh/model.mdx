### Model（模型）

模型是测试及其用于阈值的参数的组合。一个模型必须有下边界、上边界或同时具备两者。

- 下边界
  - 当较小的值表示性能退化时使用下边界，例如在`Throughput`（吞吐量）度量中。
- 上边界
  - 当较大的值表示性能退化时使用上边界，例如在`Latency`（延迟）度量中。

每个边界用于计算一个边界限制。然后每个新的指标都将与每个边界限制进行比较。当新的指标低于下边界限制或高于上边界限制时，生成一个警报。请参见[阈值](/zh/docs/explanation/thresholds/)以获取完整概述。