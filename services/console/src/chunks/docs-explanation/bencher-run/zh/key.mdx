### `--key <KEY>`

<br />

可选：`--key` 选项或 `BENCHER_API_KEY` 环境变量可以设置为有效的项目 API 密钥。
如果两者都指定，`--key` 选项优先于 `BENCHER_API_KEY` 环境变量。
项目 API 密钥限定于单个项目，只能用于 `bencher run` 和其他非破坏性命令。
项目 API 密钥不能用于创建新项目或认领已存在的项目。
与 [`--token` 选项](#--token-token)冲突。
