## 🕸 Adaptateur de Benchmark JavaScript

L'Adaptateur de Benchmark JavaScript (`js_benchmark`) attend une sortie de [Benchmark.js](https://github.com/bestiejs/benchmark.js).
La mesure de `throughput` (c'est-à-dire `opérations / seconde (ops/sec)`) est recueillie.
Les valeurs `lower_value` et `upper_value` sont la marge d'erreur relative en dessous et au-dessus de la médiane (c'est-à-dire `value`) respectivement.