[package]
name = "bencher_vsock_send"
version.workspace = true
authors.workspace = true
edition.workspace = true
license-file.workspace = true
publish = false
description = "Minimal vsock sender for guest VMs"

# This binary is designed to be statically compiled for use inside guest VMs.
# Build with: cargo build --release --target x86_64-unknown-linux-musl

[[bin]]
name = "vsock-send"
path = "src/main.rs"

[features]
default = []
plus = []

[dependencies]
libc = { workspace = true }

[lints]
workspace = true
