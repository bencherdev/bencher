### ブランチ
ブランチは、レポートを実行する際に使用される `git` の参照です（例：ブランチ名やタグ）。<code><a href="/ja/docs/explanation/bencher-run/">bencher run</a></code> CLI サブコマンドを使用する場合、ブランチが指定されていない場合は、利用可能であれば現在の `git` ブランチ名が使用されます。利用できない場合は、デフォルトで `main` が使用されます。全体的な概要については、[ブランチ選択](/ja/docs/explanation/branch-selection/)を参照してください。
