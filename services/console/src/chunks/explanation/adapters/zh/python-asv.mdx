## 🐍 Python ASV

Python ASV 适配器（`python_asv`）期望 [airspeed velocity](https://github.com/airspeed-velocity/asv) CLI [asv run](https://asv.readthedocs.io/en/stable/commands.html#asv-run) 输出。
将收集 `延迟` 测量值（即 `纳秒 (ns)`）。
`lower_value` 和 `upper_value` 分别是中位数（即 `value`）以下和以上的四分位距。