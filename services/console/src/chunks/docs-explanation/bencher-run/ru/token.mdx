### `--token <TOKEN>`

<br />

Необходимо установить либо опцию `--token`, либо переменную окружения `BENCHER_API_TOKEN` для указания действительного API токена.
Если оба определены, то значение опции `--token` имеет приоритет перед переменной окружения `BENCHER_API_TOKEN`.