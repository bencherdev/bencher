### Servicio

Seleccione los contenedores a ejecutar. Las opciones posibles son: `all`, `api` y `console`. Por defecto, se elige `all`. Similar al argumento `SERVICE` para [`docker compose logs`][docker compose logs].

[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/