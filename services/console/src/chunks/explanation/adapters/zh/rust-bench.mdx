import CargoRustBench from "../cargo-rust-bench.mdx";

## 🦀 Rust 基准测试

Rust 基准测试适配器（`rust_bench`）期望 [libtest bench](https://doc.rust-lang.org/rustc/tests/index.html#benchmarks) 输出。
收集的`延迟`度量（即`纳秒（ns）`）。
`lower_value` 和 `upper_value` 分别是低于和高于中位数（即`value`）的偏差。

<CargoRustBench />