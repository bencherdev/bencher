---
export const prerender = false;

import PerfResource from "../../../../layouts/perf/PerfResource.astro";
import benchmarksPubConfig from "../../../../config/project/benchmarks_pub";
---

<PerfResource
  title={(json) => json?.name ?? "Public Benchmark"}
  description={(json) => `View a continuous benchmarking testbed ${json?.name ? `${json?.name} ` : ""}on Bencher`}
  created={(json) => json?.created ?? ""}
  modified={(json) => json?.modified ?? ""}
  config={benchmarksPubConfig}
/>
