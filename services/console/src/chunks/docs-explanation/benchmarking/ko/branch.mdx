### 브랜치
브랜치는 리포트를 실행할 때 사용되는 `git` 참조입니다 (예: 브랜치 이름 또는 태그).
<code><a href="/ko/docs/explanation/bencher-run/">bencher run</a></code> CLI 서브 명령어를 사용할 때,
브랜치가 지정되지 않으면 현재 `git` 브랜치 이름이 사용 가능한 경우 사용됩니다.
그렇지 않으면 기본 브랜치로 `main`이 사용됩니다.
전체 개요는 [브랜치 선택](/ko/docs/explanation/branch-selection/)을 참조하세요.
