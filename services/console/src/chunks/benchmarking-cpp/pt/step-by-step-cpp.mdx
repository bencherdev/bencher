import FizzBuzzOutput from "../fizz-buzz-output.mdx";

### Siga o Passo a Passo

Para seguir este tutorial passo a passo, você precisará [instalar o `git`](https://git-scm.com/downloads), [instalar o `cmake`](https://cmake.org/download/#latest) e [instalar o GNU Compiler Collection (GCC) `g++`](https://gcc.gnu.org/install/).

> 🐰 O código-fonte deste post está [disponível no GitHub](https://github.com/bencherdev/bencher/tree/main/examples/cpp/google_benchmark).

Crie um arquivo C++ chamado `game.cpp`,
e defina seu conteúdo para a implementação FizzBuzz acima.

Use `g++` para criar um executável chamado `game` e depois executá-lo.
A saída deve se parecer com:

<FizzBuzzOutput />

> 🐰 Boom! Você está arrasando na entrevista de programação!

Antes de prosseguir, é importante discutir as diferenças entre micro-benchmarking e macro-benchmarking.