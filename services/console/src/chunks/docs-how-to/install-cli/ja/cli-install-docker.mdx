import CliDocker from "../cli-docker.mdx";
import CliDockerVersion from "../cli-docker-version.mdx";

## Docker

[Docker](https://docs.docker.com/engine/install/)コンテナを使用してBencher CLIを実行します。

<CliDocker />

任意のバージョンタグを使用してBencher CLIの具体的なバージョンを指定することができます。
それ以外の場合、最新のCLIバージョンがデフォルトとして使用されます。
特定のバージョンを指定するのは、Bencher *Self-Hosted* を使用している時だけです。
まだたまに互換性がない変更があるため、Bencher *Cloud* を使用している場合は具体的なバージョンを指定しないでください。

<CliDockerVersion />
