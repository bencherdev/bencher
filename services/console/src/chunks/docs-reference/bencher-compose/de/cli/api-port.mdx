### `--api-port <PORT>`

Geben Sie eine Portnummer für den API-Server-Container an. Standardmäßig wird der Port `61016` verwendet. Ähnlich zur Port-only-Form der [`--expose`-Option][docker run expose] für `docker run`.

[docker run expose]: https://docs.docker.com/reference/cli/docker/container/run/#publish