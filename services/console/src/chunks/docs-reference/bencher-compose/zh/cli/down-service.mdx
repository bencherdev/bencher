### 服务

选择要运行的容器。
可以选择的项有：`all`、`api` 和 `console`。
默认选择`all`。
类似于 [`docker compose down`][docker compose logs] 的 `SERVICE` 参数。

[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/