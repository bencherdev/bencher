---
export const prerender = false;

import BaseLayout from "../layouts/BaseLayout.astro";
import Help from "../components/console/help/Help";
import HelpRedirect from "../components/console/help/HelpRedirect";

const title = "Help";
const titleFmt = (title: string) => `🛟 ${title}`;
const description = "Get help with Bencher and continuous benchmarking";
---

<BaseLayout title={title} titleFmt={titleFmt} description={description}>
  <HelpRedirect client:only="solid-js" />
  <Help client:only="solid-js" />
</BaseLayout>
