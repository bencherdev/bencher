A Branch — это `git` ссылка (например, имя ветки или тег).
При использовании подкоманды CLI <code><a href="/ru/docs/explanation/bencher-run/">bencher run</a></code>,
выберите ветку с помощью опции `--branch` или переменной окружения `BENCHER_BRANCH`.
По умолчанию все проекты начинают с ветки `main`, и
`main` используется по умолчанию, если ни одна из них не указана.

При [непрерывном бенчмаркинге][continuous benchmarking], то есть бенчмаркинге в CI,
вы часто захотите создать или перебазировать `ветку` на уже существующей ветке.
Это называется Начальной точкой [Start Point][start point].
Начальная точка — это другая ветка на определенной версии (и `git` hash, если доступно).
При использовании подкоманды CLI <code><a href="/ru/docs/explanation/bencher-run/">bencher run</a></code>,
выберите Начальную точку с помощью опции `--start-point`.
Дополнительно используйте опцию `--start-point-hash` для указания конкретного `git` hash для Начальной точки.
Количество данных, которые копируются из Начальной точки, контролируется с помощью опции `--start-point-max-versions`.
Аналогично, [Пороги][thresholds] могут быть скопированы из Начальной точки с помощью флага `--start-point-clone-thresholds`.
Чтобы предотвратить отклонение, используйте флаг `--start-point-reset`, чтобы всегда сбрасывать Ветку на Начальную точку.

[continuous benchmarking]: /ru/docs/explanation/continuous-benchmarking/
[start point]: /ru/docs/explanation/benchmarking/#start-point
[thresholds]: /ru/docs/explanation/thresholds/