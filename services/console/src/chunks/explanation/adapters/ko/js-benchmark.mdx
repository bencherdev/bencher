## 🕸 JavaScript 벤치마크

JavaScript 벤치마크 어댑터(`js_benchmark`)는 [Benchmark.js](https://github.com/bestiejs/benchmark.js) 출력을 기대합니다.
`throughput` 측정치(즉, `초당 연산(ops/sec)`)가 수집됩니다.
`lower_value`와 `upper_value`는 각각 중앙값(즉, `value`) 아래와 위의 상대적 오차 범위입니다.