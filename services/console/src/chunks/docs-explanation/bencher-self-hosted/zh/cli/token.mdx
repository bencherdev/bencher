### `--token <TOKEN>`

> 🐰 已弃用：API 令牌已被弃用，请改用 API 密钥。
> 无法再创建新的 API 令牌；现有的 API 令牌仍可继续使用。
> 请改用`--key`选项。

可以将`--token`选项或`BENCHER_API_TOKEN`环境变量设置为一个有效的API令牌。如果两者都已指定，则`--token`选项优先于`BENCHER_API_TOKEN`环境变量。
