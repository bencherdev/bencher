### `plus.disaster_recovery`

Cette section spécifie la configuration de la récupération en cas de sinistre.
Bencher prend en charge la réplication continue de tous les changements de base de données.
Pour exécuter des sauvegardes à la demande ou programmées, consultez la section `database.data_store` de la configuration.

Il existe trois `schéma`s de réplication :
- `file`: Répliquer vers un chemin de fichier local
    - `path`: Chemin de réplication
- `sftp`: Répliquer via SFTP
    - `host`: Nom d'hôte du système cible
    - `port`: Numéro de port du système cible
    - `user`: Nom d'utilisateur sur le système cible
    - `password`: (Optionnel) Mot de passe sur le système cible
    - `path`: (Optionnel) Chemin sur le système cible
    - `key_path`: (Optionnel) Chemin vers la clé SSH
- `s3`: Répliquer vers un stockage de blob compatible S3
    - `bucket`: Nom du bucket
    - `path`: (Optionnel) Chemin dans le bucket
    - `endpoint`: (AWS: Optionnel | Non-AWS: Requis) Point de terminaison de réplication
    - `region`: (Optionnel) Région du bucket
    - `access_key_id`: Clé d'accès S3
    - `secret_access_key`: Clé d'accès secret S3

Les trois schémas de réplique ont les options supplémentaires suivantes :

- `snapshot.interval`: (Optionnel) Spécifie la fréquence de création des nouveaux snapshots. Cela permet de réduire le temps de restauration car les snapshots plus récents auront moins de trames WAL à appliquer. La rétention s'applique toujours à ces snapshots. Si vous ne définissez pas d'intervalle de snapshot, un nouveau snapshot sera créé chaque fois que la rétention sera effectuée. La rétention se produit toutes les 24 heures par défaut.
- `snapshot.retention`: (Optionnel) La durée pendant laquelle les fichiers snapshot & WAL seront conservés. Après la période de rétention, un nouveau snapshot sera créé et l'ancien sera supprimé. Les fichiers WAL existants avant le snapshot le plus ancien seront également supprimés. La valeur par défaut est `24h`.
- `validation.interval`: (Optionnel) Lorsqu'il est spécifié, Bencher restaurera automatiquement et validera que les données sur la réplique correspondent à la copie locale. Désactivé par défaut. Activer cela peut augmenter considérablement le coût d'exécution de Bencher car la plupart des services cloud facturent les téléchargements.
- `sync_interval`: (Optionnel) Fréquence à laquelle les trames sont poussées vers la réplique. La valeur par défaut est `1s`. Augmenter la fréquence peut augmenter considérablement les coûts de stockage cloud.
- `checkpoint.interval`: (Optionnel) Fréquence à laquelle Litestream effectue un checkpoint PASSIVE non bloquant indépendamment du nombre de pages. Litestream ignore le checkpoint si des lecteurs ou écrivains sont actifs. La valeur par défaut est `1m`.
- `checkpoint.min_page_count`: (Optionnel) Nombre minimum de pages WAL avant qu'un checkpoint PASSIVE non bloquant ne soit déclenché (~4Ko par page). Litestream ignore le checkpoint si des lecteurs ou écrivains sont actifs. La valeur par défaut est `1000` (~4Mo).
- `checkpoint.truncate_page_n`: (Optionnel) Seuil de pages pour un checkpoint TRUNCATE bloquant. Lorsque ce seuil est atteint, Litestream effectue un checkpoint bloquant qui attend la fin de tous les lecteurs et écrivains, ce qui peut provoquer des erreurs `database is locked` en cas de charge. **La valeur par défaut est `0` (désactivé).** Ne définissez une valeur non nulle que si vous avez besoin d'une limite supérieure sur la taille du fichier WAL et comprenez les implications du blocage.

|            Nom             | Exemples  | Défaut | Requis |                                              Description                                              |
| :------------------------: | :-------: | :----: | :----: | :---------------------------------------------------------------------------------------------------: |
|          replica           | \{ ... \} |  ---   |  Oui   |                                      Spécifie un objet réplique.                                      |
|       replica.scheme       |   "s3"    |  ---   |  Oui   | Spécifie le schéma de réplication. Pour toutes les autres clés de `replica`, voir la liste ci-dessus. |
|     snapshot.interval      |   "1h"    |  ---   |  Non   | Fréquence de création des nouveaux snapshots. Réduit le temps de restauration. |
|     snapshot.retention     |  "24h"    |  24h   |  Non   | Durée de conservation des fichiers snapshot et WAL avant remplacement. |
|    validation.interval     |   "6h"    |  ---   |  Non   | Fréquence de restauration et validation des données de réplique par rapport à la copie locale. Désactivé par défaut. |
|    checkpoint.interval     |   "1m"    |   1m   |  Non   | Fréquence d'exécution d'un checkpoint PASSIVE non bloquant. Ignoré si des lecteurs/écrivains sont actifs. |
| checkpoint.min_page_count  |   1000    |  1000  |  Non   | Pages WAL minimales (~4Ko chacune) avant un checkpoint PASSIVE. Ignoré si des lecteurs/écrivains sont actifs. |
| checkpoint.truncate_page_n |     0     |   0    |  Non   | Seuil de pages pour un checkpoint TRUNCATE bloquant. **0 désactive.** Les valeurs non nulles peuvent provoquer `database is locked` en charge. |