import RunCmdExample from "../run-cmd-example.mdx";

If you had a script located at `./run/my/benchmarks.sh` that ran your benchmarks
and printed the results to standard out as BMF JSON,
then you could track them using
<code><a href="/docs/explanation/bencher-run/">bencher run</a></code>
and [the `json` adapter][json adapter]
with the following command:

<RunCmdExample />

[json adapter]: /docs/explanation/adapters/#-json
