import RunCmdExample from "../run-cmd-example.mdx";

もし `./run/my/benchmarks.sh`に位置するスクリプトを持っていて、それがベンチマークを実行し、BMF JSONとして標準出力に結果を出力した場合、
以下のコマンドを使用して、
<code><a href="/ja/docs/explanation/bencher-run/">bencher run</a></code>
および[the `json` adapter][json adapter]を使用して追跡できます。

<RunCmdExample />

[json adapter]: /ja/docs/explanation/adapters/#-json