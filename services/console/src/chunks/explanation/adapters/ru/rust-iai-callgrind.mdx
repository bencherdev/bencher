import CargoRustIaiCallgrind from "../cargo-rust-iai-callgrind.mdx";

## 🦀 Rust Iai-Callgrind

Адаптер Rust Iai (`rust_iai_callgrind`) работает с выводом [Iai-Callgrind](https://github.com/iai-callgrind/iai-callgrind).
Собираются данные по `instructions`, `l1_access`, `l2_access`, `ram_access`, `total_accesses` и `estimated_cycles`.
Доступны только эти измерения (т.е. `value`). Нет измерений `lower_value` и `upper_value`.
Измерения для этого адаптера не создаются автоматически для всех проектов.
Однако, при использовании этого адаптера, эти измерения будут автоматически созданы для вашего проекта.

<CargoRustIaiCallgrind />