import DockerCompose from "../docker-compose.mdx";
import BencherUp from "./bencher-up.mdx";
import BencherLogs from "./bencher-logs.mdx";
import BencherDown from "./bencher-down.mdx";

## デプロイ

Bencher Self-Hosted をデプロイする最も簡単な方法は、`bencher` CLI のサブコマンドの三連画です：

- [`bencher up`][bencher up]: Bencher Self-Hosted のコンテナを起動します。
  これは [`docker compose up`][docker compose up] を実行するのと似ています。
- [`bencher logs`][bencher logs]: Bencher Self-Hosted のコンテナからの出力を表示します。
  これは `bencher up` が[ `--detach` フラグ][detach flag] で実行された場合に必要です。
  これは [`docker compose logs`][docker compose logs] を実行するのと似ています。
- [`bencher down`][bencher down]: Bencher Self-Hosted のコンテナを停止して削除します。
  これは `bencher up` が[ `--detach` フラグ][detach flag] で実行された場合に必要です。
  これは [`docker compose down`][docker compose down] を実行するのと似ています。

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/
[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/
[docker compose down]: https://docs.docker.com/reference/cli/docker/compose/down/

[bencher up]: #bencher-up
[bencher logs]: #bencher-logs
[bencher down]: #bencher-down
[detach flag]: #--detach

<div class="box">
  <BencherUp />
</div>

<div class="box">
  <BencherLogs />
</div>

<div class="box">
  <BencherDown />
</div>

あるいは、`docker compose` を使用してコンテナをオーケストレーションすることもできます。
以下は `docker-compose.yml` ファイルの例です：

<DockerCompose />