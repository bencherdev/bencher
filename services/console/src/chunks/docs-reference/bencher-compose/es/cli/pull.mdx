### `--pull <WHEN>`

Extraer imágenes antes de ejecutar. Las opciones posibles son: `always`, `missing` y `never`. Por defecto, se elige `always`. Similar a la opción `--pull` para [`docker compose up`][docker compose up].

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/