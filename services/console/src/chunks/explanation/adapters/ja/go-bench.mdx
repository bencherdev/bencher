## 🕳 Goベンチ

Go Benchアダプター (`go_bench`) は [go test -bench](https://pkg.go.dev/testing#hdr-Benchmarks) の出力を期待しています。
`latency` 測定値（すなわち `ナノ秒 (ns)`）が収集されます。
`value`（すなわち平均値）のみが利用可能です。`lower_value` と `upper_value` はありません。