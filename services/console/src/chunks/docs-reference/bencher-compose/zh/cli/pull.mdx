### `--pull <WHEN>`

在运行之前拉取镜像。
可能的选项有：`always`、`missing` 和 `never`。
默认情况下选择 `always`。
类似于 [`docker compose up`][docker compose up] 的 `--pull` 选项。

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/