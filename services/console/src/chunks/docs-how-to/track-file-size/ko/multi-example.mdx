import MultiExample from "../multi-example.mdx";
import MultiJsonExample from "../multi-json-example.mdx";

## 여러 파일 크기

여러 파일의 크기를 추적하려면,
동일한 <code><a href="/ko/docs/explanation/bencher-run/">bencher run</a></code> 명령에서 여러 번 `--file-size` 옵션을 사용하면 됩니다.
예를 들어, `my_binary`와 Windows 실행 파일 버전인 `my_binary.exe`의 크기를 추적할 수 있습니다.

<MultiExample />

`my_binary.exe`의 크기가 `48` 바이트인 경우 생성된 BMF JSON은 다음과 같습니다:

<MultiJsonExample />
