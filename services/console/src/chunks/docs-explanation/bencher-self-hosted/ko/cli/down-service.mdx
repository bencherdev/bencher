### 서비스

실행할 컨테이너를 선택하세요.
가능한 선택지는 `all`, `api`, `console`입니다.
기본적으로 `all`이 선택됩니다.
[`docker compose down`][docker compose logs]의 `SERVICE` 인수와 유사합니다.

[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/