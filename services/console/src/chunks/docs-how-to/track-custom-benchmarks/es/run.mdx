import RunCmdExample from "../run-cmd-example.mdx";

Si tuvieras un script ubicado en `./run/my/benchmarks.sh` que ejecutara tus referencias de medición
y mostrara los resultados en la salida estándar como JSON de BMF,
entonces podrías rastrearlos usando
<code><a href="/es/docs/explanation/bencher-run/">bencher run</a></code>
y [el adaptador `json`][json adapter]
con el siguiente comando:

<RunCmdExample />

[json adapter]: /es/docs/explanation/adapters/#-json