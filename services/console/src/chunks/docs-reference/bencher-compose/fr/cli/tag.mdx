import { BENCHER_VERSION } from "../../../../../util/ext";

### `--tag <TAG>`

Spécifiez le tag de l'image pour les conteneurs. Par défaut, le tag de l'image correspond à la version du CLI `bencher`. La version la plus récente du CLI `bencher` est <code>{`v${BENCHER_VERSION}`}</code>.