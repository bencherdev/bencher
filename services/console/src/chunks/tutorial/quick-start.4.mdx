import { BENCHER_VERSION } from "../../util/ext";

<pre><code>{`$ cargo install --git https://github.com/bencherdev/bencher --tag ${BENCHER_VERSION} --locked bencher_cli`}
{`  Installing bencher_cli v${BENCHER_VERSION} (/workspace/bencher/services/cli)`}
    Updating crates.io index
    ...
    Finished release [optimized] target(s) in 0.27s
  Installing /workspace/.cargo/bin/bencher
{`   Installed package \`bencher_cli v${BENCHER_VERSION} (/workspace/bencher/services/cli)\` (executable \`bencher\`)`}</code></pre>
