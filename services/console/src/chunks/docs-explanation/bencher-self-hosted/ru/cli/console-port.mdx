### `--console-port <PORT>`

Укажите номер порта для контейнера Console сервера.
По умолчанию используется порт `3000`.
Аналогично форме только с портом для [опции `--expose`][docker run expose] для `docker run`.

[docker run expose]: https://docs.docker.com/reference/cli/docker/container/run/#publish