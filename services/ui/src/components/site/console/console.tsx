export enum Operation {
  LIST,
  ADD,
  VIEW,
  EDIT,
  DELETE,
  PERF,
}

export enum Button {
  ADD,
  REFRESH,
  BACK,
}

export enum Resource {
  PROJECTS,
}

export enum Field {
  INPUT,
  TEXTAREA,
  CHECKBOX,
  SWITCH,
  SELECT,
}
