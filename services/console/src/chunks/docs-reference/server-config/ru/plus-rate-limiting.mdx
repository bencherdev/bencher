### `plus.rate_limiting`

Этот раздел определяет ограничения скорости для сервера API.
Весь раздел является необязательным.
Если не указано, будут использоваться перечисленные значения по умолчанию.

Чтобы предотвратить создание [`unclaimed` проектов][unclaimed projects],
установите `unclaimed_limit` на `0`.

|          Имя           |    Пример    | По умолчанию | Обязательно |                                                                               Описание                                                                               |
| :--------------------: | :----------: | :----------: | :---------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------: |
|         window         |    86400     |    86400     |     Нет     |                                       Определяет окно ограничения скорости в секундах. По умолчанию установлено на один день.                                        |
|    unclaimed_limit     |     255      |     255      |     Нет     | Контролирует ограничение скорости для [`unclaimed` проектов][unclaimed projects]. Чтобы предотвратить создание `unclaimed` проектов, установите это значение на `0`. |
|     claimed_limit      |    65536     |    65536     |     Нет     |                                                      Контролирует ограничение скорости для `claimed` проектов.                                                       |

#### `public`

Ограничитель скорости `public` применяется к каждому IP-адресу для неаутентифицированных запросов.
Каждое подполе содержит ограничения `minute`, `hour` и `day`.
Весь раздел является необязательным.
Если не указано, будут использоваться перечисленные значения по умолчанию.

|       Имя        |  Пример  | По умолчанию | Обязательно |                           Описание                                             |
| :--------------: | :------: | :----------: | :---------: | :----------------------------------------------------------------------------: |
|     requests     | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости запросов по IP-адресу.                           |
| requests.minute  |  1024   |    1024     |     Нет     | Максимум запросов в минуту на IP-адрес.                                          |
|  requests.hour   |  4096   |    4096     |     Нет     | Максимум запросов в час на IP-адрес.                                             |
|   requests.day   |  8192   |    8192     |     Нет     | Максимум запросов в день на IP-адрес.                                            |
|     attempts     | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости попыток аутентификации по IP-адресу.              |
| attempts.minute  |   32    |     32      |     Нет     | Максимум попыток аутентификации в минуту на IP-адрес.                            |
|  attempts.hour   |   64    |     64      |     Нет     | Максимум попыток аутентификации в час на IP-адрес.                               |
|   attempts.day   |   128   |     128     |     Нет     | Максимум попыток аутентификации в день на IP-адрес.                              |
|       runs       | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости запусков бенчмарков по IP-адресу для невостребованных проектов. |
|   runs.minute    |   64    |     64      |     Нет     | Максимум запусков бенчмарков в минуту на IP-адрес.                               |
|    runs.hour     |   128   |     128     |     Нет     | Максимум запусков бенчмарков в час на IP-адрес.                                  |
|     runs.day     |   256   |     256     |     Нет     | Максимум запусков бенчмарков в день на IP-адрес.                                 |

#### `user`

Ограничитель скорости `user` применяется к каждому аутентифицированному пользователю.
Каждое подполе содержит ограничения `minute`, `hour` и `day`.
Весь раздел является необязательным.
Если не указано, будут использоваться перечисленные значения по умолчанию.

|         Имя          |  Пример  | По умолчанию | Обязательно |                           Описание                                             |
| :------------------: | :------: | :----------: | :---------: | :----------------------------------------------------------------------------: |
|       requests       | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости запросов на пользователя.                        |
|   requests.minute    |  2048   |    2048     |     Нет     | Максимум запросов в минуту на пользователя.                                      |
|    requests.hour     |  8192   |    8192     |     Нет     | Максимум запросов в час на пользователя.                                          |
|     requests.day     |  16384  |    16384    |     Нет     | Максимум запросов в день на пользователя.                                         |
|       attempts       | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости попыток аутентификации на пользователя.            |
|   attempts.minute    |    2    |      2      |     Нет     | Максимум попыток аутентификации в минуту на пользователя.                         |
|    attempts.hour     |    4    |      4      |     Нет     | Максимум попыток аутентификации в час на пользователя.                            |
|     attempts.day     |    8    |      8      |     Нет     | Максимум попыток аутентификации в день на пользователя.                           |
|     credentials      | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости создания учётных данных на пользователя.           |
|  credentials.minute  |    4    |      4      |     Нет     | Максимум созданий учётных данных в минуту на пользователя.                        |
|   credentials.hour   |    8    |      8      |     Нет     | Максимум созданий учётных данных в час на пользователя.                           |
|   credentials.day    |   16    |     16      |     Нет     | Максимум созданий учётных данных в день на пользователя.                          |
|    organizations     | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости создания организаций на пользователя.              |
| organizations.minute |    2    |      2      |     Нет     | Максимум созданий организаций в минуту на пользователя.                           |
|  organizations.hour  |    4    |      4      |     Нет     | Максимум созданий организаций в час на пользователя.                              |
|  organizations.day   |    8    |      8      |     Нет     | Максимум созданий организаций в день на пользователя.                             |
|       invites        | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости приглашений на пользователя.                      |
|    invites.minute    |    8    |      8      |     Нет     | Максимум приглашений в минуту на пользователя.                                   |
|     invites.hour     |   16    |     16      |     Нет     | Максимум приглашений в час на пользователя.                                      |
|     invites.day      |   32    |     32      |     Нет     | Максимум приглашений в день на пользователя.                                     |
|         runs         | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости запусков бенчмарков на пользователя.               |
|     runs.minute      |   64    |     64      |     Нет     | Максимум запусков бенчмарков в минуту на пользователя.                            |
|      runs.hour       |  1024   |    1024     |     Нет     | Максимум запусков бенчмарков в час на пользователя.                               |
|       runs.day       |  4096   |    4096     |     Нет     | Максимум запусков бенчмарков в день на пользователя.                              |

#### `project`

Ограничитель скорости `project` применяется к каждому проекту.
Каждое подполе содержит ограничения `minute`, `hour` и `day`.
Весь раздел является необязательным.
Если не указано, будут использоваться перечисленные значения по умолчанию.

|       Имя        |  Пример  | По умолчанию | Обязательно |                           Описание                                             |
| :--------------: | :------: | :----------: | :---------: | :----------------------------------------------------------------------------: |
|     requests     | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости запросов на проект.                              |
| requests.minute  |  2048   |    2048     |     Нет     | Максимум запросов в минуту на проект.                                            |
|  requests.hour   |  8192   |    8192     |     Нет     | Максимум запросов в час на проект.                                               |
|   requests.day   |  16384  |    16384    |     Нет     | Максимум запросов в день на проект.                                              |
|       runs       | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости запусков бенчмарков на проект.                    |
|   runs.minute    |   64    |     64      |     Нет     | Максимум запусков бенчмарков в минуту на проект.                                 |
|    runs.hour     |  1024   |    1024     |     Нет     | Максимум запусков бенчмарков в час на проект.                                    |
|     runs.day     |  4096   |    4096     |     Нет     | Максимум запусков бенчмарков в день на проект.                                   |

#### `runner`

Ограничитель скорости `runner` применяется к каждому раннеру.
Каждое подполе содержит ограничения `minute`, `hour` и `day`.
Весь раздел является необязательным.
Если не указано, будут использоваться перечисленные значения по умолчанию.

|       Имя        |  Пример  | По умолчанию | Обязательно |                           Описание                                             |
| :--------------: | :------: | :----------: | :---------: | :----------------------------------------------------------------------------: |
|     requests     | \{ ... \} |     ---     |     Нет     | Определяет ограничения скорости запросов на раннер.                              |
| requests.minute  |   16    |     16      |     Нет     | Максимум запросов в минуту на раннер.                                            |
|  requests.hour   |   256   |     256     |     Нет     | Максимум запросов в час на раннер.                                               |
|   requests.day   |  4096   |    4096     |     Нет     | Максимум запросов в день на раннер.                                              |

#### `oci_bandwidth`

`oci_bandwidth` определяет дневные лимиты пропускной способности OCI-реестра для каждого уровня организации.
Весь раздел является необязательным.
Если не указано, будут использоваться перечисленные значения по умолчанию.

|     Имя     |    Пример    | По умолчанию | Обязательно |                           Описание                                                        |
| :---------: | :----------: | :----------: | :---------: | :---------------------------------------------------------------------------------------: |
|  unclaimed  | 1073741824   | 1073741824   |     Нет     | Максимум байт в день для невостребованных организаций (0 участников). По умолчанию 1 ГиБ.   |
|    free     | 10737418240  | 10737418240  |     Нет     | Максимум байт в день для бесплатных (востребованных, без платного плана) организаций. По умолчанию 10 ГиБ. |
|    plus     | 107374182400 | 107374182400 |     Нет     | Максимум байт в день для организаций Plus (Team/Enterprise). По умолчанию 100 ГиБ.         |

[unclaimed projects]: /ru/docs/how-to/claim/#unclaimed-projects
