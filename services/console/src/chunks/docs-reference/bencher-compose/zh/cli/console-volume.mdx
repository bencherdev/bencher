### `--console-volume <HOST_CONTAINER>`

将挂载卷传递给控制台服务器容器。
预期格式为 `/host/path:/container/path`。
这在使用[控制台服务器配置][console config]时可能很有用。
类似于 `docker run` 的[`--volume` 选项][docker run volume]。

[docker run volume]: https://docs.docker.com/reference/cli/docker/container/run/#volume

[console config]: /zh/docs/reference/console-config/