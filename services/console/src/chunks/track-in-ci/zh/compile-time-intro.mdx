编译时间可能是构建时间的重要组成部分，特别是在{props.lang}中。[持续基准测试][continuous benchmarking]是在每个变更集上运行基准测试的做法，以确保变更不会引入性能回退。可以将编译时间视为基准测试，并在 CI 中跟踪它们以捕捉编译时间过长的问题。为{props.lang}编译时间实现持续基准测试的最简单方法是使用 Bencher。

[continuous benchmarking]: /zh/docs/explanation/continuous-benchmarking/