## ♦️ Ruby 基准测试

Ruby 基准测试适配器（`ruby_benchmark`）期望 [Benchmark 模块](https://github.com/ruby/benchmark) 对于 `#bm`、`#bmbm`、和 `#benchmark` 方法的输出。
每个基准测试都需要一个标签。
将收集 `latency` 测量值（即 `纳秒 (ns)`）。
只提供报告值（即 `value`），没有 `lower_value` 和 `upper_value`。