### `--api-port <PORT>`

Укажите номер порта для контейнера API-сервера.
По умолчанию используется порт `61016`.
Аналогично форме только с портом для параметра [`--expose`][docker run expose] в `docker run`.

[docker run expose]: https://docs.docker.com/reference/cli/docker/container/run/#publish