### `--pull <WHEN>`

Puxa as imagens antes de executar.
As opções possíveis são: `always`, `missing` e `never`.
Por padrão, `always` é escolhido.
Semelhante à opção `--pull` para [`docker compose up`][docker compose up].

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/