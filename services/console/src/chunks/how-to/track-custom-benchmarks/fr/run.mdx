import RunCmdExample from "../run-cmd-example.mdx";

Si vous aviez un script situé à `./run/my/benchmarks.sh` qui exécutait vos benchmarks
et imprimait les résultats sur la sortie standard en tant que JSON BMF,
alors vous pourriez les suivre en utilisant
<code><a href="/fr/docs/explanation/bencher-run/">bencher run</a></code>
et [l'adaptateur `json`][json adapter]
avec la commande suivante :

<RunCmdExample />

[json adapter]: /fr/docs/explanation/adapters/#-json