### Branch
Branch 是运行报告时使用的 `git` 引用（即分支名称或标签）。默认情况下，所有项目都以 `main` 分支开始。在使用<code><a href="/zh/docs/explanation/bencher-run/">bencher run</a></code> CLI 子命令时，如果未提供分支，则默认使用 `main` 分支。请参阅[分支选择](/zh/docs/explanation/branch-selection/)以获取完整概述。