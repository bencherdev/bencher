import CargoBench from "../cargo-bench.mdx";
import CargoBenchJson from "../cargo-bench-json.mdx";

## Das benutzerdefinierte Benchmark-Harness ausführen

Alles ist nun bereit.
Wir können endlich unser benutzerdefiniertes Benchmark-Harness ausführen.

<CargoBench />

Die Ausgabe sowohl in der Standardausgabe
als auch in einer Datei namens `results.json` sollte so aussehen:

<CargoBenchJson />

Die genauen Zahlen, die Sie sehen, können je nach Architektur Ihres Computers etwas anders sein.
Aber das Wichtige ist, dass Sie zumindest einige Werte für die letzten vier Metriken haben.