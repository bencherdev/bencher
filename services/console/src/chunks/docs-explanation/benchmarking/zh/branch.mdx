### 分支
分支是运行报告时使用的 `git` 引用（即分支名称或标签）。
使用<code><a href="/zh/docs/explanation/bencher-run/">bencher run</a></code> CLI 子命令时，如果未指定分支，则使用当前的 `git` 分支名称（如果可用）。否则，`main` 将作为默认分支。
请参阅[分支选择](/zh/docs/explanation/branch-selection/)以获取完整概述。
