### `--console-port <PORT>`

Consoleサーバーコンテナのポート番号を指定します。
デフォルトでは、ポート`3000`が使用されます。
`docker run`の[`--expose`オプション][docker run expose]のポートのみの形式と類似しています。

[docker run expose]: https://docs.docker.com/reference/cli/docker/container/run/#publish