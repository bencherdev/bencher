export enum Operation {
  LIST,
  ADD,
  VIEW,
  EDIT,
  DELETE,
  PERF,
}

export enum Button {
  ADD,
  REFRESH,
  BACK,
}

export enum Resource {
  PROJECTS,
  REPORTS,
  BRANCHES,
  TESTBEDS,
  CONNECTIONS,
  PROJECT_SETTINGS,
  USER_ACCOUNT,
  USER_SETTINGS,
}

export enum Field {
  INPUT,
  TEXTAREA,
  CHECKBOX,
  SWITCH,
  SELECT,
}

export enum Row {
  TEXT,
  BOOL,
}
