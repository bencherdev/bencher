import BencherRunCode from "../bencher-run-code.mdx";

## 호스트 URL

Bencher Self-Hosted를 사용할 때,
대부분의 `bencher` CLI 하위 명령어에 대해
Bencher API 서버의 URL로 [the `--host` option][host option]을 설정해야 합니다.

예를 들어, `https://api.bencher.example.com`에 있는 Bencher API 서버와 함께 <code><a href="/ko/docs/explanation/bencher-run/">bencher run</a></code> CLI 하위 명령어를 사용한다면 다음과 같이 작성할 수 있습니다:

<BencherRunCode />

[host option]: /ko/docs/explanation/bencher-run/#--host-url