Bencher Console UI는 구성 설정이 필요하지 않습니다.
그러나 다음 환경 변수를 사용하여 필요에 맞게 기본값을 사용자 지정할 수 있습니다:

- `BENCHER_API_URL`: [Bencher API 서버][server config]의 URL입니다.
  기본적으로 `http://localhost:61016`로 설정되어 있습니다.
- `INTERNAL_API_URL`: `docker compose`와 같은 Docker 기반 네트워킹을 사용할 때,
  로컬 `BENCHER_API_URL`을 사용할 경우 Docker 간 통신에 필요합니다.
  `https://bencher.example.com`과 같은 원격 `BENCHER_API_URL`을 사용할 경우에는 설정하지 마십시오.
  기본적으로 설정되지 않습니다.
- `GITHUB_CLIENT_ID`: "GitHub로 로그인"을 활성화하기 위한 [Self-Hosted GitHub App][github app]의 GitHub Client ID입니다. 이는 [Bencher Plus Enterprise 플랜][bencher plus enterprise]을 필요로 합니다. 기본적으로 설정되지 않습니다.

[server config]: /ko/docs/reference/server-config/
[github app]: /ko/docs/how-to/github-app/
[bencher plus enterprise]: /pricing/?level=enterprise