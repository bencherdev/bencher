### `--console-port <PORT>`

Geben Sie eine Portnummer für den Console-Servercontainer an.
Standardmäßig wird Port `3000` verwendet.
Ähnlich wie das Port-Only-Format der [Option `--expose`][docker run expose] für `docker run`.

[docker run expose]: https://docs.docker.com/reference/cli/docker/container/run/#publish