import MakeCppCatch2 from "../make-cpp-catch2.mdx";

## ➕ C++ Catch2

C++ Catch2 适配器（`cpp_catch2`）期望 [Catch2](https://github.com/catchorg/Catch2) 输出。
将收集 `latency` 度量（即 `纳秒 (ns)`）。
`lower_value` 和 `upper_value` 分别是低于和高于均值（即 `value`）的一个标准差。

<MakeCppCatch2 />