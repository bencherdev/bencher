---
import { Language, defaultLang } from "../../i18n/ui";

const { lang } = Astro.props;
const language = lang ?? defaultLang;

const title = (lang: Language) => {
	switch (lang) {
		case Language.de:
			return "Ihre nächste Performance-Regression kündigt sich nicht selbst an";
		case Language.en:
			return "Your next performance regression won't announce itself";
		case Language.es:
			return "Tu próxima regresión de rendimiento no se anunciará sola";
		case Language.fr:
			return "Votre prochaine régression de performance ne s'annoncera pas toute seule";
		case Language.ja:
			return "あなたの次のパフォーマンス回帰は自ら知らせてはくれません";
		case Language.ko:
			return "당신의 다음 성능 회귀는 스스로 알리지 않습니다";
		case Language.pt:
			return "Sua próxima regressão de desempenho não vai se anunciar";
		case Language.ru:
			return "Ваша следующая регрессия производительности не объявит о себе";
		case Language.zh:
			return "你的下一次性能回归不会自己提醒你";
	}
};

const description = (lang: Language) => {
	switch (lang) {
		case Language.en:
			return "Catch performance regressions in your CI. Free to start.";
		case Language.es:
			return "Detecta regresiones de rendimiento en tu CI. Gratis para empezar.";
		case Language.fr:
			return "Attrapez les régressions de performance dans votre CI. Gratuit pour commencer.";
		case Language.de:
			return "Fangen Sie Performance-Regressionen in Ihrer CI ab. Kostenlos starten.";
		case Language.ja:
			return "CIでパフォーマンス回帰をキャッチ。無料で始められます。";
		case Language.ko:
			return "CI에서 성능 회귀를 잡으세요. 무료로 시작.";
		case Language.pt:
			return "Capture regressões de desempenho no seu CI. Grátis para começar.";
		case Language.ru:
			return "Ловите регрессии производительности в вашем CI. Бесплатно для старта.";
		case Language.zh:
			return "在您的 CI 中捕捉性能回归。免费开始。";
	}
};

const primaryCta = (lang: Language) => {
	switch (lang) {
		case Language.de:
			return "Kostenlos mit Benchmarking starten";
		case Language.en:
			return "Start benchmarking free";
		case Language.es:
			return "Empieza a hacer benchmarking gratis";
		case Language.fr:
			return "Commencez le benchmarking gratuitement";
		case Language.ja:
			return "無料でベンチマークを開始";
		case Language.ko:
			return "무료로 벤치마킹 시작";
		case Language.pt:
			return "Comece a fazer benchmarking grátis";
		case Language.ru:
			return "Начать бенчмаркинг бесплатно";
		case Language.zh:
			return "免费开始基准测试";
	}
};
---

<section class="cta-section">
	<div class="columns is-centered">
		<div class="column is-10 has-text-centered">
			<h2 class="title is-1 cta-title">{title(language)}</h2>
			<p class="subtitle is-5 cta-sub">{description(language)}</p>
			<div class="cta-buttons">
				<a class="cta-primary" href="/auth/signup">
					{primaryCta(language)}
				</a>
			</div>
		</div>
	</div>
</section>

<style>
	.cta-section {
		padding: 6rem 1.5rem 8rem;
	}

	@media screen and (min-width: 769px) {
		.cta-section {
			padding: 7rem 2rem 9rem;
		}
	}

	@media screen and (min-width: 1024px) {
		.cta-section {
			padding: 8rem 4rem 10rem;
		}
	}

	.cta-title {
		margin-bottom: 1.25rem !important;
		max-width: 900px;
		margin-left: auto !important;
		margin-right: auto !important;
	}

	.cta-sub {
		max-width: 640px;
		margin: 0 auto 2.5rem !important;
		color: var(--landing-muted);
	}

	.cta-buttons {
		display: flex;
		justify-content: center;
		gap: 1rem;
		flex-wrap: wrap;
	}

	.cta-primary,
	.cta-secondary {
		display: inline-flex;
		align-items: center;
		padding: 0.75rem 1.4rem;
		border-radius: 8px;
		font-size: 1rem;
		font-weight: 500;
		text-decoration: none;
		transition: background 0.15s, border-color 0.15s, color 0.15s;
	}

	.cta-primary {
		background: #ed6704;
		color: #ffffff;
		border: 1px solid #ed6704;
	}

	.cta-primary:hover {
		background: #d25a00;
		border-color: #d25a00;
	}

	.cta-secondary {
		background: transparent;
		color: inherit;
		border: 1px solid rgba(0, 0, 0, 0.2);
	}

	.cta-secondary:hover {
		background: rgba(0, 0, 0, 0.04);
	}

	:global([data-theme="dark"]) .cta-secondary {
		border-color: rgba(255, 255, 255, 0.2);
	}

	:global([data-theme="dark"]) .cta-secondary:hover {
		background: rgba(255, 255, 255, 0.05);
	}
</style>
