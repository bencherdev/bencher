import { BENCHER_VERSION } from "../../../../../util/ext";

### `--tag <TAG>`

コンテナのイメージタグを指定します。 デフォルトでは、イメージタグは `bencher` CLI のバージョンと一致します。最新の `bencher` CLI のバージョンは <code>{`v${BENCHER_VERSION}`}</code> です。