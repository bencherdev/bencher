import Go from "../go.mdx";

## 🕳 Go Bench

L'adapteur Go Bench (`go_bench`) attend la sortie de [go test -bench](https://pkg.go.dev/testing#hdr-Benchmarks).
La mesure de `latence` (c'est-à-dire `nanosecondes (ns)`) est recueillie.
Seule la moyenne (c'est-à-dire `valeur`) est disponible. Il n'y a pas de `lower_value` et de `upper_value`.

<Go />