### `--key <KEY>`

Soit l'option `--key`, soit la variable d'environnement `BENCHER_API_KEY` peut être définie avec une clé API utilisateur valide (`bencher_user_*`). Si les deux sont spécifiées, l'option `--key` prévaut sur la variable d'environnement `BENCHER_API_KEY`.