---
export const prerender = false;

import ConsoleLayout from "../../../../../layouts/ConsoleLayout.astro";
import PosterPanel from "../../../../../components/console/poster/PosterPanel";
import { Resource } from "../../../../../config/types";

const path = "/console/users/:user_slug/tokens/add";
const title = "Add Token";
---

<ConsoleLayout path={path} title={title}>
  <PosterPanel client:only path={path} resource={Resource.TOKENS} />
</ConsoleLayout>
