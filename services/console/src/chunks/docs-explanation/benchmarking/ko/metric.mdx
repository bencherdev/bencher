### Metric
Metric은 특정 시점의 성능 퇴보 테스트 결과를 나타내는 단일 값입니다. 하나의 Metric에 대해 최대 세 가지 값인 `value`, `lower_value` 및 `upper_value`를 수집할 수 있습니다. `value`는 모든 Metric에서 필수이며, `lower_value`와 `upper_value`는 각각 독립적으로 선택 사항입니다. 어떤 값들이 수집되는지는 [벤치마크 하네스 어댑터](/ko/docs/explanation/adapters/)에 의해 결정됩니다.