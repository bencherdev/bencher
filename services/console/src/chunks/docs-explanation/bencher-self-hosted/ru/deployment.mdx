import DockerCompose from "../docker-compose.mdx";
import BencherUp from "./bencher-up.mdx";
import BencherLogs from "./bencher-logs.mdx";
import BencherDown from "./bencher-down.mdx";

## Развертывание

Самый простой способ развернуть Bencher Self-Hosted — использовать триптих подкоманд CLI `bencher`:

- [`bencher up`][bencher up]: Запустить контейнеры Bencher Self-Hosted.
  Это похоже на выполнение команды [`docker compose up`][docker compose up].
- [`bencher logs`][bencher logs]: Просмотр вывода из контейнеров Bencher Self-Hosted,
  если `bencher up` был выполнен с [флагом `--detach`][detach flag].
  Это похоже на выполнение команды [`docker compose logs`][docker compose logs].
- [`bencher down`][bencher down]: Остановить и удалить контейнеры Bencher Self-Hosted,
  если `bencher up` был выполнен с [флагом `--detach`][detach flag].
  Это похоже на выполнение команды [`docker compose down`][docker compose down].

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/
[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/
[docker compose down]: https://docs.docker.com/reference/cli/docker/compose/down/

[bencher up]: #bencher-up
[bencher logs]: #bencher-logs
[bencher down]: #bencher-down
[detach flag]: #--detach

<div class="box">
  <BencherUp />
</div>

<div class="box">
  <BencherLogs />
</div>

<div class="box">
  <BencherDown />
</div>

В качестве альтернативы, вы можете использовать `docker compose` для оркестрации контейнеров.
Это пример файла `docker-compose.yml`:

<DockerCompose />