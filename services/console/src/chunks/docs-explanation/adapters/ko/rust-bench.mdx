import CargoRustBench from "../cargo-rust-bench.mdx";

## 🦀 러스트 벤치

러스트 벤치 어댑터(`rust_bench`)는 [libtest 벤치](https://doc.rust-lang.org/rustc/tests/index.html#benchmarks) 출력을 기대합니다.
`지연 시간` 측정값(즉, `나노초(ns)`)이 수집됩니다.
`lower_value`와 `upper_value`는 각각 중앙값(즉, `value`) 아래와 위의 편차입니다.

<CargoRustBench />