import CargoRustIaiCallgrind from "../cargo-rust-iai-callgrind.mdx";

## 🦀 Rust Iai-Callgrind

El adaptador de Rust Iai (`rust_iai_callgrind`) espera la salida de [Iai-Callgrind](https://github.com/iai-callgrind/iai-callgrind).
Se recopilan las medidas de `instructions`, `l1_access`, `l2_access`, `ram_access`, `total_accesses` y `estimated_cycles`.
Solo estas medidas (es decir, `value`) están disponibles. No hay medidas de `lower_value` ni `upper_value`.
Las medidas para este adaptador no se crean por defecto para todos los proyectos.
Sin embargo, cuando utilizas este adaptador, estas medidas se crearán automáticamente para tu proyecto.

<CargoRustIaiCallgrind />