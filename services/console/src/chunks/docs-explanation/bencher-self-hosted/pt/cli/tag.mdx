import { BENCHER_VERSION } from "../../../../../util/ext";

### `--tag <TAG>`

Especifique a tag da imagem para os containers. Por padrão, a tag da imagem corresponde à versão do CLI `bencher`. A versão mais recente do CLI `bencher` é <code>{`v${BENCHER_VERSION}`}</code>.