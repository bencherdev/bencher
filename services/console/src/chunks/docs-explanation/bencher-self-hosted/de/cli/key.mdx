### `--key <KEY>`

Entweder muss die Option `--key` oder die Umgebungsvariable `BENCHER_API_KEY` auf einen gültigen Benutzer-API-Schlüssel (`bencher_user_*`) gesetzt werden.
Wenn beide angegeben sind, hat die Option `--key` Vorrang vor der Umgebungsvariable `BENCHER_API_KEY`.
