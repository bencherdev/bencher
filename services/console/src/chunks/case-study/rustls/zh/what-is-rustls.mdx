## 什么是 Rustls？
[Rustls][rustls] 是一个用 Rust 编写的现代化传输层安全协议（TLS）库，
旨在替换诸如 OpenSSL 这样的非内存安全替代品。
TLS 协议通常用于提供安全通讯，
主要在网络服务器和客户端之间。
TLS 之前叫做安全套接字层 (SSL)。
它确保了在两方之间传输的数据被加密，
并且安全无法被窃听或篡改。
因此，像 Rustls 这样的 TLS 库既要快速也要保证安全。

> 🐰 在 `https` 中的 `s` 表示你正在使用 TLS 来查看此页面！

[rustls]: https://github.com/rustls/rustls