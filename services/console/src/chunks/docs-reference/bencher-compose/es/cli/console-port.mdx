### `--console-port <PORT>`

Especifica un número de puerto para el contenedor del servidor de la Consola.
Por defecto, se utiliza el puerto `3000`.
Similar a la forma solo puerto de [la opción `--expose`][docker run expose] para `docker run`.

[docker run expose]: https://docs.docker.com/reference/cli/docker/container/run/#publish