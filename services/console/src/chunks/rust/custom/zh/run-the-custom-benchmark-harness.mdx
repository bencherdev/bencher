import CargoBench from "../cargo-bench.mdx";
import CargoBenchJson from "../cargo-bench-json.mdx";

## 运行自定义基准测试工具

一切都准备就绪。
我们终于可以运行自定义基准测试工具了。

<CargoBench />

输出到标准输出和名为 `results.json` 的文件的内容应如下所示：

<CargoBenchJson />

你看到的确切数字可能会根据你的计算机架构略有不同。
但重要的是你至少应该有最后四个指标的一些值。