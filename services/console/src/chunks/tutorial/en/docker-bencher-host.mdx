import DockerBencherHostOutput from "../docker-bencher-host-output.mdx";

## Set Bencher Host

The `bencher` CLI default host is Bencher Cloud (ie https://api.bencher.dev).
So we need to set your hostname. The easiest way to do this is the with `BENCHER_HOST` environment variable.

Open a new terminal window.

On Linux, Mac, and other Unix-like systems run: `export BENCHER_HOST=http://localhost:61016`

On Windows run: `$env:BENCHER_HOST = "http://localhost:61016"`

If you then run `echo $BENCHER_HOST` or `Write-Output $env:BENCHER_HOST` respectively.

You should see:

<DockerBencherHostOutput />