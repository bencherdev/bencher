import Go from "../go.mdx";

## 🕳 Go Bench

O Adaptador Go Bench (`go_bench`) espera um output de [go test -bench](https://pkg.go.dev/testing#hdr-Benchmarks).
A Medida de `latência` (ou seja, `nanosegundos (ns)`) é coletada.
Apenas a média (ou seja, `valor`) está disponível. Não existem `valor_inferior` e `valor_superior`.

<Go />