import CliUnixScript from "../../general/cli-unix-script.mdx";
import CliWindowsScript from "../../general/cli-windows-script.mdx";
import QSCliVersionOutput from "../qs-cli-version-output.mdx";

## `bencher` CLIのインストール

### Linux、Mac、およびUnix
Linux、Mac、およびその他のUnix系システムでは、ターミナルで以下を実行します：

<CliUnixScript />

### Windows
Windowsシステムでは、以下のように_PowerShell_ターミナルで実行します：

<CliWindowsScript />

<br />

> 🐰 `このシステムではスクリプトの実行が無効化されています`というエラーが表示される場合：
> - `管理者として実行`で`Powershellを開く`
> - 以下を実行： `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned`
> - 次に入力：`Y`
> - このスクリプトを再実行


追加のインストールオプションについては、[`bencher` CLIのインストール方法](/ja/docs/how-to/install-cli)を参照してください。

今度は、`bencher` CLIがインストールされていることをテストしましょう。

実行: `bencher --version`

次のように表示されるはずです:

<QSCliVersionOutput />
