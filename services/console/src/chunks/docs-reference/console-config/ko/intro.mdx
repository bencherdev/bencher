Bencher Console UI는 별도의 설정이 필요하지 않습니다.
다만, 다음 환경 변수를 사용하여 일부 기본값을 필요에 맞게 사용자화할 수 있습니다:

- `BENCHER_API_URL`: [Bencher API 서버][server config]의 URL입니다.
  기본값은 `http://localhost:61016`로 설정되어 있습니다.
- `INTERNAL_API_URL`: `docker compose`와 같은 Docker 기반 네트워킹을 사용할 때,
  로컬 `BENCHER_API_URL`을 사용할 경우 Docker-대-Docker 통신을 위해 필요합니다.
  `https://bencher.example.com`과 같은 원격 `BENCHER_API_URL`을 사용하는 경우에는 설정하지 마십시오.
  기본적으로 설정되어 있지 않습니다.
- `OAUTH_GITHUB`: [Self-Hosted GitHub App][github app]을 사용한 `Login with GitHub`을 활성화하려면 `true`로 설정하세요.
  이 기능은 [Bencher Plus Enterprise 플랜][bencher plus enterprise]이 필요합니다. 기본적으로 설정되어 있지 않습니다.
- `OAUTH_GOOGLE`: [Self-Hosted Google OAuth2][google oauth2]를 사용한 `Login with Google`을 활성화하려면 `true`로 설정하세요.
  이 기능은 [Bencher Plus Enterprise 플랜][bencher plus enterprise]이 필요합니다. 기본적으로 설정되어 있지 않습니다.

[server config]: /ko/docs/reference/server-config/
[github app]: /ko/docs/how-to/github-app/
[bencher plus enterprise]: /pricing/?level=enterprise
[google oauth2]: /ko/docs/how-to/google-oauth/
