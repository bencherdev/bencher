import FileCmdExample from "../file-cmd-example.mdx";
import FileNoCmdExample from "../file-no-cmd-example.mdx";

Si vos résultats étaient stockés dans un fichier nommé `results.json`,
alors vous pourriez utiliser l'option `--file` pour spécifier le chemin du fichier.
Cela fonctionne à la fois avec [une commande de benchmark][bencher run] et sans.

Avec une commande de benchmark :
<FileCmdExample />

Sans une commande de benchmark :
<FileNoCmdExample />

[bencher run]: /fr/docs/explanation/bencher-run/#benchmark-command