import BmfExample from "../bmf-example.mdx";
import BmfSchema from "../bmf-schema.mdx";

## \{...\} JSON

Адаптер JSON (`json`) ожидает JSON в формате BMF.
Он идеально подходит для интеграции пользовательских систем измерения производительности с Bencher.

Пример BMF:

<BmfExample />

В этом примере ключ `benchmark_name` будет названием теста производительности.
Имена тестов могут быть любой непустой строкой, до 1024 символов.
Объект `benchmark_name` содержит названия измерений, их идентификаторы или UUID в качестве ключей.
В данном примере `latency` является идентификатором для измерения задержки.
По умолчанию в каждом проекте есть измерения задержки (то есть `latency`) и пропускной способности (то есть `throughput`),
которые измеряются в `наносекундах (ns)` и `операциях в секунду (ops/s)` соответственно.
Объект измерения содержит метрику с тремя возможными значениями: `value`, `lower_value` и `upper_value`.
Значения `lower_value` и `upper_value` необязательны,
и их расчет специфичен для системы измерения производительности.

В этом примере объект измерения `latency` содержит следующие значения:

- `value` равное `88.0`
- `lower_value` равное `87.42`
- `upper_value` равное `88.88`

Если JSON в формате BMF сохранен в файле,
то вы можете использовать подкоманду CLI <code><a href="/ru/docs/explanation/bencher-run/">bencher run</a></code> с необязательным аргументом `--file` для указания пути к этому файлу.
Это работает как с командой запуска теста производительности (например, `bencher run --file results.json "bencher mock > results.json"`)
так и без команды запуска теста (например, `bencher mock > results.json && bencher run --file results.json`).

<BmfSchema />

> 🐰 Примечание: Подкоманда CLI `bencher mock` генерирует макетные метрики BMF.