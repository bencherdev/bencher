import CliDocker from "../cli-docker.mdx";
import CliDockerVersion from "../cli-docker-version.mdx";

## Docker

Führen Sie die Bencher CLI mit einem [Docker](https://docs.docker.com/engine/install/) Container aus.

<CliDocker />

Es gibt ein optionales Versions-Tag, um eine genaue Version der Bencher CLI zu verwenden.
Andernfalls wird standardmäßig die neueste CLI-Version verwendet.
Geben Sie eine genaue Version an, wenn und nur wenn Bencher *Self-Hosted* verwendet wird.
Geben Sie __nicht__ eine genaue Version an, wenn Sie Bencher *Cloud* verwenden, da es noch gelegentlich zu Kompatibilitätsproblemen kommt.

<CliDockerVersion />
