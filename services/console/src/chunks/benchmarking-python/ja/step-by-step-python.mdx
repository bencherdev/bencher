import FizzBuzzOutput from "../fizz-buzz-output.mdx";

### 手順を順を追ってフォロー

この手順ごとのチュートリアルをフォローするには、[Pythonをインストール](https://www.python.org/downloads/)し、[ `pipenv` をインストール](https://pipenv.pypa.io/en/latest/installation.html)する必要があります。

> 🐰 この投稿のソースコードは[GitHubで利用可能](https://github.com/bencherdev/bencher/tree/main/examples/python/pytest_benchmark)です。

`game.py`という名前のPythonファイルを作成し、その内容を上記のFizzBuzz実装に設定します。

次に、`python game.py`を実行します。
出力は次のようになります：

<FizzBuzzOutput />

> 🐰 バム！コーディングインタビューを突破していますね！

さらに進む前に、マイクロベンチマークとマクロベンチマークの違いについて話し合うことが重要です。