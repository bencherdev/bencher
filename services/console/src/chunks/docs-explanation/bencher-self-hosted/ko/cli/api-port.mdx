### `--api-port <PORT>`

API 서버 컨테이너에 대한 포트 번호를 지정합니다. 기본적으로 포트 `61016`이 사용됩니다. `docker run`의 [`--expose` 옵션][docker run expose]의 포트 전용 형식과 유사합니다.

[docker run expose]: https://docs.docker.com/reference/cli/docker/container/run/#publish