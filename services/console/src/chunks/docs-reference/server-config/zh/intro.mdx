import Example from "../example.mdx";

Bencher API 服务器在启动时需要一个 JSON 配置。
这个配置可以通过以下三种方式提供：
- `BENCHER_CONFIG` 环境变量：该值应设置为 JSON 配置
- `BENCHER_CONFIG_PATH` 环境变量：该值应设置为包含 JSON 配置文件的路径
- `/etc/bencher/bencher.json` 文件：此位置的文件包含 JSON 配置

如果没有找到配置，则加载默认配置。

示例 JSON 配置：

<Example />