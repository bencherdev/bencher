import FileCmdExample from "../file-cmd-example.mdx";
import FileNoCmdExample from "../file-no-cmd-example.mdx";

Wenn Ihre Ergebnisse stattdessen in einer Datei namens `results.json` gespeichert wurden,
dann könnten Sie die Option `--file` verwenden, um den Dateipfad anzugeben.
Dies funktioniert sowohl mit [einem Benchmark-Befehl][bencher run] als auch ohne einen.

Mit einem Benchmark-Befehl:
<FileCmdExample />

Ohne einen Benchmark-Befehl:
<FileNoCmdExample />

[bencher run]: /de/docs/explanation/bencher-run/#benchmark-command