### `--pull <WHEN>`

Tirez les images avant de les exécuter. Les choix possibles sont : `always`, `missing`, et `never`. Par défaut, `always` est choisi. Semblable à l'option `--pull` pour [`docker compose up`][docker compose up].

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/