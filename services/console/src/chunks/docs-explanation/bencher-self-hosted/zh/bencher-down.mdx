import BencherDownCmd from "../bencher-down-cmd.mdx";
import DownService from "./cli/down-service.mdx";
import Help from "./cli/help.mdx";

## `bencher down`

停止并移除 Bencher 自托管的容器，
如果 `bencher up` 时使用了[ `--detach` 标志][detach flag]。
这类似于运行 [`docker compose down`][docker compose down]。

<BencherDownCmd />

[docker compose down]: https://docs.docker.com/reference/cli/docker/compose/down/

[detach flag]: #--detach

<DownService />

### 选项

<Help />