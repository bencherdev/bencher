import BmfExample from "../bmf-example.mdx";

Ceci est un exemple de JSON BMF :

<BmfExample />

Dans cet exemple, la clé `benchmark_name` correspondrait au nom d'un [Benchmark][benchmark].
Un Benchmark peut être identifié par son nom, son slug ou son UUID.
L'objet `benchmark_name` peut contenir plusieurs clés correspondant à des noms, des slugs ou des UUID de [Measure][measure].
Si la valeur spécifiée est un nom ou un slug et que la Measure n'existe pas encore, elle sera créée pour vous.
En revanche, si la valeur spécifiée est un UUID, la Measure doit déjà exister.
Dans cet exemple, `latency` est le slug de la Measure Latency intégrée.
Chaque Project a par défaut une Measure Latency (c.-à-d. `latency`) et une Measure Throughput (c.-à-d. `throughput`),
qui sont mesurées respectivement en `nanosecond (ns)` et en `operations / second (ops/s)`.
L'objet Measure contient un [Metric][metric] ayant jusqu'à trois valeurs : `value`, `lower_value` et `upper_value`.
Les valeurs `lower_value` et `upper_value` sont optionnelles.

Dans cet exemple, l'objet Measure `latency` contient les valeurs suivantes :

- Une `value` de `88.0`
- Une `lower_value` de `87.42`
- Une `upper_value` de `88.88`

[benchmark]: /fr/docs/explanation/benchmarking/#benchmark
[measure]: /fr/docs/explanation/benchmarking/#measure
[metric]: /fr/docs/explanation/benchmarking/#metrics
