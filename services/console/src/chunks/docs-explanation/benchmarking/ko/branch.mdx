### Branch
Branch는 보고서를 실행할 때 사용되는 `git` 참조입니다 (즉, 브랜치 이름 또는 태그).
기본적으로 모든 프로젝트는 `main` 브랜치로 시작합니다.
<code><a href="/ko/docs/explanation/bencher-run/">bencher run</a></code> CLI 하위 명령어를 사용할 때,
`main`은 제공된 브랜치가 없을 경우 기본 브랜치입니다.
[브랜치 선택](/ko/docs/explanation/branch-selection/)에서 전체 개요를 확인하세요.