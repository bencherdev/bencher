## ➕ C++ Catch2

C++ Catch2 어댑터(`cpp_catch2`)는 [Catch2](https://github.com/catchorg/Catch2) 출력을 예상합니다.
`지연시간` 측정(즉 `나노초(ns)`)이 수집됩니다.
`lower_value`와 `upper_value`는 각각 평균(즉 `value`)보다 한 표준 편차 아래와 위입니다.