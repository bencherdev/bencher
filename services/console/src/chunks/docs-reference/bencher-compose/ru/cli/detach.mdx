### `--detach`

Запуск в отсоединённом режиме. Контейнеры будут запущены в фоновом режиме. Похоже на флаг `--detach` для [`docker compose up`][docker compose up].

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/