---
export const prerender = false;

import PerfResource from "../../../../layouts/perf/PerfResource.astro";
import branchesPubConfig from "../../../../config/project/branches_pub";
---

<PerfResource
  title={(json) => json?.name ?? "Public Branch"}
  description={(json) => `View a continuous benchmarking branch ${json?.name ? `${json?.name} ` : ""}on Bencher`}
  created={(json) => json?.created ?? ""}
  modified={(json) => json?.modified ?? ""}
  config={branchesPubConfig}
/>
