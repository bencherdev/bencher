import FizzBuzzRules from "../../benchmarking/en/fizz-buzz-rules.mdx";
import FizzBuzzPythonCode from "../fizz-buzz-python-code.mdx";

## Write FizzBuzz in Python

<FizzBuzzRules />

<FizzBuzzPythonCode />

- Create a Python file, `game.py`.
- Iterate from `1` to `100`, using a range of `101`.
- For each number, calculate the modulus (remainder after division) for both `3` and `5`.
- If the remainder is `0`, then the number is a multiple of the given factor.
  - If the remainder is `0` for `15`, then print `FizzBuzz`.
  - If the remainder is `0` for `3`, then print `Fizz`.
  - If the remainder is `0` for `5` ,then print `Buzz`.
- Otherwise, just print the number.
