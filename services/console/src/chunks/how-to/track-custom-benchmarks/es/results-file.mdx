import FileCmdExample from "../file-cmd-example.mdx";
import FileNoCmdExample from "../file-no-cmd-example.mdx";

Si sus resultados se almacenaran en un archivo llamado `results.json`,
entonces podría usar la opción `--file` para especificar la ruta del archivo.
Esto funciona tanto con [un comando de benchmark][bencher run] como sin uno.

Con un comando de benchmark:
<FileCmdExample />

Sin un comando de benchmark:
<FileNoCmdExample />

[bencher run]: /es/docs/explanation/bencher-run/#benchmark-command