### `--console-port <PORT>`

Especifique um número de porta para o container do servidor do Console.
Por padrão, a porta `3000` é utilizada.
Semelhante à forma apenas de porta da [opção `--expose`][docker run expose] para o `docker run`.

[docker run expose]: https://docs.docker.com/reference/cli/docker/container/run/#publish