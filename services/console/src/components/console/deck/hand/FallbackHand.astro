---
import FallbackCard from "./card/FallbackCard.astro";

const cards = Astro.props.card ?? 4;
---

{ cards > 0 && <FallbackCard /> }
{ cards > 1 && <FallbackCard /> }
{ cards > 2 && <FallbackCard /> }
{ cards > 3 && <FallbackCard /> }
{ cards > 4 && <FallbackCard /> }
{ cards > 5 && <FallbackCard /> }
{ cards > 6 && <FallbackCard /> }
{ cards > 7 && <FallbackCard /> }
{ cards > 8 && <FallbackCard /> }
{ cards > 9 && <FallbackCard /> }
{ cards > 10 && <FallbackCard /> }
{ cards > 11 && <FallbackCard /> }
{ cards > 12 && <FallbackCard /> }
{ cards > 13 && <FallbackCard /> }
{ cards > 14 && <FallbackCard /> }
{ cards > 15 && <FallbackCard /> }
