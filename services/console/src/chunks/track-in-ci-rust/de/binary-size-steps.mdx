import StepsForBencherCloud from "./steps-for-bencher-cloud.mdx";
import StepsForBencherSelfHosted from "./steps-for-bencher-self-hosted.mdx";

<StepsForBencherCloud
  step={<>Kompilieren Sie Ihren {props.lang} Code mit dem <code><a href="/de/docs/explanation/bencher-run/">bencher run</a></code> Unterbefehl in Ihrem CI-Workflow unter Verwendung der <a href={props.optionUrl}><code>--file-size</code> Option</a>, die auf den Ausgabepfad für Ihre Binärdatei zusammen mit dem <a href={props.adapterUrl}><code>json</code> Adapter</a> gesetzt ist.</>}
/>

<StepsForBencherSelfHosted
  step={<>Kompilieren Sie Ihren {props.lang} Code mit dem <code><a href="/de/docs/explanation/bencher-run/">bencher run</a></code> Unterbefehl in Ihrem CI-Workflow unter Verwendung der <a href={props.optionUrl}><code>--file-size</code> Option</a>, die auf den Ausgabepfad für Ihre Binärdatei zusammen mit dem <a href={props.adapterUrl}><code>json</code> Adapter</a> gesetzt ist, und richten Sie die <a href="/de/docs/explanation/bencher-run/#--host-url"><code>--host</code> Option</a> auf die URL Ihrer Bencher Self-Hosted Instanz ein.</>}
/>