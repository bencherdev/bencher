import { BENCHER_VERSION } from "../../../../../util/ext";

### `--tag <TAG>`

Укажите тег образа для контейнеров.
По умолчанию тег образа соответствует версии CLI `bencher`.
Самая последняя версия CLI `bencher` — это <code>{`v${BENCHER_VERSION}`}</code>.