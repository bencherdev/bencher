import CargoBench from "../cargo-bench.mdx";

## 🦀 Rust Iai-Callgrind

El Adaptador Rust Iai (`rust_iai_callgrind`) espera la salida de [Iai-Callgrind](https://github.com/iai-callgrind/iai-callgrind).
Se recopilan las medidas `instructions`, `l1_access`, `l2_access`, `ram_access`, `total_accesses`, y `estimated_cycles`.
Solo estas medidas (es decir, `value`) están disponibles. No hay medidas de `lower_value` y `upper_value`.
Las medidas para este adaptador no se crean por defecto para todos los proyectos.
Sin embargo, cuando usas este adaptador, estas medidas se crearán automáticamente para tu proyecto.

<CargoBench />