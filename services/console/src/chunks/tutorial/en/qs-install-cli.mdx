import CliInstallScripts from "../../general/en/cli-install-scripts.mdx";
import QSCliVersionOutput from "../qs-cli-version-output.mdx";

## Install `bencher` CLI

<CliInstallScripts />

For additional installation options, see [how to install the `bencher` CLI](/docs/how-to/install-cli).

Now, lets test that we have the `bencher` CLI installed.

Run: `bencher --version`

You should see:

<QSCliVersionOutput />
