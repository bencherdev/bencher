### `--key <KEY>`

Either the `--key` option or the `BENCHER_API_KEY` environment variable
may be set to a valid user API key (`bencher_user_*`).
If both are specified, the `--key` option takes precedence over the `BENCHER_API_KEY` environment variable.
