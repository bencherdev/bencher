import StepsForBencherCloud from "./steps-for-bencher-cloud.mdx";
import StepsForBencherSelfHosted from "./steps-for-bencher-self-hosted.mdx";

<StepsForBencherCloud
  step={<>CI 워크플로에서 <code><a href="/ko/docs/explanation/bencher-run/">bencher run</a></code> 서브 명령어를 사용하여 {props.lang} 코드를 컴파일합니다. 바이너리의 출력 경로로 설정된 <a href={props.optionUrl}> <code>--file-size</code> 옵션</a>과 함께 <a href={props.adapterUrl}> <code>json</code> 어댑터</a>를 사용하십시오.</>}
/>

<StepsForBencherSelfHosted
  step={<>CI 워크플로에서 <code><a href="/ko/docs/explanation/bencher-run/">bencher run</a></code> 서브 명령어를 사용하여 {props.lang} 코드를 컴파일합니다. 바이너리의 출력 경로로 설정된 <a href={props.optionUrl}> <code>--file-size</code> 옵션</a>과 함께 <a href={props.adapterUrl}> <code>json</code> 어댑터</a>를 사용하고 <a href="/ko/docs/explanation/bencher-run/#--host-url"> <code>--host</code> 옵션</a>을 Bencher 셀프 호스팅 인스턴스 URL로 설정하십시오.</>}
/>