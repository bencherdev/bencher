import MermaidDiagram from "../../../components/docs/MermaidDiagram.astro";
import { Diagram } from "../../../components/docs/diagram";

<MermaidDiagram
	diagram={Diagram.RunnerSetup}
	alt="Sequence diagram of self-hosting a Bencher Bare Metal Runner: create a Runner, create a Spec, assign the Spec, start the runner binary, and execute a claimed Job"
/>
