### サービス

実行するコンテナを選択します。
選択可能なオプションは、`all`、`api`、および `console` です。
デフォルトでは `all` が選択されています。
[`docker compose logs`][docker compose logs] の `SERVICE` 引数と似ています。

[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/