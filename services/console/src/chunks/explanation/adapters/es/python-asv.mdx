## 🐍 ASV de Python

El Adaptador ASV de Python (`python_asv`) espera la salida de la línea de comandos [airspeed velocity](https://github.com/airspeed-velocity/asv) CLI [asv run](https://asv.readthedocs.io/en/stable/commands.html#asv-run).
La medida de `latencia` (es decir, `nanosegundos (ns)`) es recopilada.
El `valor_inferior` y el `valor_superior` son el rango intercuartílico por debajo y por encima del mediano (es decir, `valor`) respectivamente.