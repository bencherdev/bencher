---
import DocsLayout from "./DocsLayout.astro";
import DocsTab from "../../components/docs/docs_tab";

interface Props {
  title: string;
  titleFmt?: undefined | ((title: string) => string);
  description: string;
  heading?: string;
  lang?: string;
}

const { title, titleFmt, description, heading, lang } = Astro.props;
---

<DocsLayout
  title={title}
  titleFmt={titleFmt ?? ((title) => `🧑‍💻 ${heading ?? title}`)}
  description={description}
  heading={heading}
  tab={DocsTab.Learn}
  lang={lang}
>
  <slot />
</DocsLayout>
