### `--token <TOKEN>`

> 🐰 非推奨: APIトークンは非推奨となり、APIキーに置き換えられました。
> 新しいAPIトークンは作成できなくなりましたが、既存のAPIトークンは引き続き使用できます。
> 代わりに`--key`オプションを使用してください。

`--token`オプションまたは`BENCHER_API_TOKEN`環境変数のいずれかを、有効なAPIトークンとして設定する必要があります。
両方が指定されている場合、`--token`オプションが`BENCHER_API_TOKEN`環境変数よりも優先されます。
