### `--key <KEY>`

Либо параметр `--key`, либо переменная окружения `BENCHER_API_KEY` должны быть установлены на действительный ключ API пользователя (`bencher_user_*`). Если оба параметра указаны, опция `--key` имеет приоритет над переменной окружения `BENCHER_API_KEY`.
