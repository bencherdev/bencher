enum FieldKind {
	INPUT = "input",
	NUMBER = "number",
	CHECKBOX = "checkbox",
	SWITCH = "switch",
	SELECT = "select",
	RADIO = "radio",
	MODEL = "model",
	SEARCH = "search",
	PLOT_WINDOW = "plot_window",
	PLOT_RANK = "plot_rank",
}

export default FieldKind;
