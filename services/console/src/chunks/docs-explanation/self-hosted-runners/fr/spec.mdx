## Créer un Spec

Un [Spec][spec] décrit le matériel qu'un Runner fournit :
système d'exploitation, architecture du CPU, type de [Sandbox][sandbox], nombre de CPU, mémoire, disque et accès réseau.
Un Job déclare le Spec dont il a besoin, et un Runner ne réclame que les Jobs des Specs qu'il prend en charge.

Créez un Spec avec la [CLI `bencher`][specs api],
l'[API REST Specs][specs api],
ou la Bencher Console :

```sh
bencher spec create \
  --name "Intel v1" \
  --os linux \
  --architecture x86_64 \
  --sandbox firecracker \
  --cpu 4 \
  --memory 51539607552 \
  --disk 137438953472
```

Omettez `--sandbox` pour définir un Spec sans sandbox,
et passez `--network` si les Jobs sur ce Spec sont autorisés à accéder au réseau.
La mémoire et le disque sont spécifiés en octets.
Pour en savoir plus sur les Specs et sur la façon dont un [Testbed][testbeds] enregistre le Spec utilisé pour chaque Report,
consultez [Testbeds & Specs][testbeds].

[spec]: /fr/docs/explanation/bare-metal/#spec
[sandbox]: /fr/docs/explanation/bare-metal/#sandbox
[specs api]: /fr/docs/api/server/specs/
[testbeds]: /fr/docs/explanation/testbeds/
