### `--api-port <PORT>`

Especifique um número de porta para o contêiner do servidor API.
Por padrão, a porta `61016` é usada.
Semelhante à forma apenas de porta da [opção `--expose`][docker run expose] para `docker run`.

[docker run expose]: https://docs.docker.com/reference/cli/docker/container/run/#publish