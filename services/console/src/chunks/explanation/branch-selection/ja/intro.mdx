`git`参照（つまり、ブランチ名やタグ）は`Branch`です。
<code><a href="/ja/docs/explanation/bencher-run/">bencher run</a></code> CLIサブコマンドを使用する場合、
`--branch`オプションまたは`BENCHER_BRANCH`環境変数でBranchを選択します。
デフォルトでは、すべてのプロジェクトは`main`ブランチから始まり、
指定されていない場合、デフォルトで`main`が使用されます。

[継続的ベンチマーク][continuous benchmarking]、つまりCIでのベンチマークを行う場合、
既存のブランチに基づいて`branch`を作成したり、リベースしたりすることがよくあります。
これはブランチの[スタートポイント][start point]と呼ばれます。
スタートポイントは、特定のバージョン（および利用可能であれば`git`ハッシュ）である別のブランチです。
<code><a href="/ja/docs/explanation/bencher-run/">bencher run</a></code> CLIサブコマンドを使用する場合、
`--branch-start-point`オプションでスタートポイントを選択します。
さらに、`--branch-start-point-hash`オプションを使用して、スタートポイントの特定の`git`ハッシュを指定します。
すべてのデータと[閾値][thresholds]はスタートポイントからコピーされます。

[continuous benchmarking]: /ja/docs/explanation/continuous-benchmarking/
[start point]: /ja/docs/explanation/benchmarking/#start-point
[thresholds]: /ja/docs/explanation/thresholds/