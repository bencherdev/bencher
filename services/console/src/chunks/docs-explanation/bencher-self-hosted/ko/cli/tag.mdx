import { BENCHER_VERSION } from "../../../../../util/ext";

### `--tag <TAG>`

컨테이너에 대한 이미지 태그를 지정합니다. 기본적으로 이미지 태그는 `bencher` CLI 버전과 일치합니다. 최신 `bencher` CLI 버전은 <code>{`v${BENCHER_VERSION}`}</code>입니다.