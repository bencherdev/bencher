### Serviço

Selecione os contêineres para executar.
As opções possíveis são: `all`, `api` e `console`.
Por padrão, `all` é escolhido.
Semelhante ao argumento `SERVICE` para [`docker compose logs`][docker compose logs].

[docker compose logs]: https://docs.docker.com/reference/cli/docker/compose/logs/