### サービス

実行するコンテナを選択します。
選択肢は、`all`、`api`、および `console` です。
デフォルトでは `all` が選択されます。
[`docker compose up`][docker compose up] の `SERVICE` 引数に類似しています。

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/