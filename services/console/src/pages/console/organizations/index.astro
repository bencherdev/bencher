---
import UserLayout from "../../../layouts/console/UserLayout.astro";
import TablePanel from "../../../components/console/table/TablePanel";
import { Resource } from "../../../config/types";

const params = Astro.params;
const title = "Organizations";
---

<UserLayout params={params} title={title}>
  <TablePanel client:only params={params} resource={Resource.ORGANIZATIONS} />
</UserLayout>
