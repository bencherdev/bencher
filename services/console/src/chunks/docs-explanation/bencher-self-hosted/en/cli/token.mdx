### `--token <TOKEN>`

> 🐰 DEPRECATED: API tokens are deprecated in favor of API keys.
> New API tokens can no longer be created; existing API tokens continue to work.
> Use the `--key` option instead.

Either the `--token` option or the `BENCHER_API_TOKEN` environment variable
may be set to a valid API token.
If both are specified, the `--token` option takes precedence over the `BENCHER_API_TOKEN` environment variable.
