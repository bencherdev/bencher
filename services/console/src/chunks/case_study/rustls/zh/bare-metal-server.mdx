#### 裸金属服务器

为了获得他们在墙时间基准测试上的1%的分辨率，Rustls项目投资了一台[特殊配置的][服务器配置]裸金属持续基准测试服务器。与大多数现代CI运行者不同，这台服务器并非短暂存在。也就是说，每次运行都使用相同的底层服务器硬件和操作系统。没有虚拟化。

裸金属服务器已经特别配置，以便创建尽可能一致的结果。频率缩放（Intel 的 TurboBoost）和同时多线程（Intel的超线程）在 BIOS 中都已禁用。CPU缩放设置为`性能`。通过在 `sysctl.conf`中设置`kernel.randomize_va_space=0` 和 `kernel.nmi_watchdog=0`， 分别禁用了地址空间布局随机化（ASLR）和非屏蔽中断（NMI）看门狗。裸金属服务器由[OVHcloud][OVHcloud]托管。

[服务器配置]: https://github.com/rust-lang/rustc-perf/blob/master/docs/zh/perf-runner.md
[OVHcloud]: https://www.ovhcloud.com/en/