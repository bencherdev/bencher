### `--pull <WHEN>`

Загрузка образов перед запуском. Возможные варианты: `always`, `missing` и `never`. По умолчанию выбран вариант `always`. Аналогично опции `--pull` для [`docker compose up`][docker compose up].

[docker compose up]: https://docs.docker.com/reference/cli/docker/compose/up/