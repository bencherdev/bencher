### `--key <KEY>`

Ou a opção `--key` ou a variável de ambiente `BENCHER_API_KEY` deve ser configurada com uma chave de API de usuário válida (`bencher_user_*`). Se ambas forem especificadas, a opção `--key` tem precedência sobre a variável de ambiente `BENCHER_API_KEY`.
