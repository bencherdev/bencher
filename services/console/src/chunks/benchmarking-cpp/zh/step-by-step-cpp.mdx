import FizzBuzzOutput from "../fizz-buzz-output.mdx";

### 按步骤操作

为了按照这个逐步教程进行操作，您需要[安装 `git`](https://git-scm.com/downloads)，[安装 `cmake`](https://cmake.org/download/#latest)，以及[安装 GNU 编译器集合 (GCC) `g++`](https://gcc.gnu.org/install/)。

> 🐰 本文的源码可在 [GitHub 上找到](https://github.com/bencherdev/bencher/tree/main/examples/cpp/google_benchmark)。

创建一个名为 `game.cpp` 的 C++ 文件，
并将其内容设置为上述 FizzBuzz 实现。

使用 `g++` 构建一个名为 `game` 的可执行文件，然后运行它。
输出应如下所示：

<FizzBuzzOutput />

> 🐰 砰! 你在破解编码面试！

在继续进行之前，讨论微基准测试和宏基准测试之间的区别是很重要的。