### メトリック
メトリックは、単一の時点でのパフォーマンス回帰テストの結果を示します。1つのメトリックに対して、最大3つの値を収集することができます: `value`、`lower_value`、および `upper_value`。`value`はすべてのメトリックに必須ですが、`lower_value`と`upper_value`は独立してオプションです。どの値が収集されるかは、[ベンチマークハーネスアダプタ](/ja/docs/explanation/adapters/)によって決定されます。